# kerf-cad Feature Catalog

_Auto-generated by `cargo run -p kerf-cad --bin extract_catalog`. Do not edit by hand — change `crates/kerf-cad/src/feature.rs` doc comments instead._

This catalog covers **241** `Feature` variants. Each entry lists its parameters and a default JSON skeleton you can paste into a `Model`. Examples that take an `input` (transforms, edge ops, holes) assume a sibling feature with id `"body"` (a 10x10x10 `Box`) exists in the model; booleans assume both `"body"` and `"tool"` exist.

## Categories

- [Primitives](#primitives) (20)
- [Extrusions & profiles](#extrusions--profiles) (4)
- [Sweeps & paths](#sweeps--paths) (4)
- [Booleans](#booleans) (3)
- [Transforms & patterns](#transforms--patterns) (7)
- [Edge treatments (fillet/chamfer/cut)](#edge-treatments-filletchamfercut) (4)
- [Holes & pockets](#holes--pockets) (16)
- [Fasteners (bolts, screws, rivets, etc.)](#fasteners-bolts-screws-rivets-etc) (14)
- [Joinery (mortise/tenon/dovetail)](#joinery-mortisetenondovetail) (5)
- [Structural shapes (beams, channels, brackets)](#structural-shapes-beams-channels-brackets) (67)
- [Vessels & containers](#vessels--containers) (16)
- [Furniture](#furniture) (10)
- [Tools & hardware](#tools--hardware) (23)
- [Reference geometry & markers](#reference-geometry--markers) (11)
- [Decorative & miscellaneous shapes](#decorative--miscellaneous-shapes) (37)

## Primitives

### `Box`

_(undocumented - add a doc comment to the variant in `feature.rs`.)_

**Parameters**

| field | type |
|---|---|
| `extents` | `[Scalar; 3]` |

**Example**

```json
{
  "extents": [
    10.0,
    10.0,
    10.0
  ],
  "id": "box",
  "kind": "Box"
}
```

### `BoxAt`

_(undocumented - add a doc comment to the variant in `feature.rs`.)_

**Parameters**

| field | type |
|---|---|
| `extents` | `[Scalar; 3]` |
| `origin` | `[Scalar; 3]` |

**Example**

```json
{
  "extents": [
    10.0,
    10.0,
    10.0
  ],
  "id": "box_at",
  "kind": "BoxAt",
  "origin": [
    0.0,
    0.0,
    0.0
  ]
}
```

### `Capsule`

Capsule (cylinder with hemispherical caps) along +z. The cylindrical body has `radius` and `height`, sitting at z ∈ [0, h]. Two hemispheres of `radius` cap the bottom (z=0) and top (z=`height`). Total length along z = height + 2*radius. `stacks` and `slices` set the sphere tessellation; the cylinder uses `slices` for its lateral faceting too.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `height` | `Scalar` |
| `stacks` | `usize` |
| `slices` | `usize` |

**Example**

```json
{
  "height": 10.0,
  "id": "capsule",
  "kind": "Capsule",
  "radius": 5.0,
  "slices": 16,
  "stacks": 16
}
```

### `CapsuleAt`

CapsuleAt: like Capsule but with a chosen axis-aligned orientation. `axis` is "x" / "y" / "z".

**Parameters**

| field | type |
|---|---|
| `axis` | `String` |
| `center` | `[Scalar; 3]` |
| `radius` | `Scalar` |
| `body_length` | `Scalar` |
| `stacks` | `usize` |
| `slices` | `usize` |

**Example**

```json
{
  "axis": "z",
  "body_length": 10.0,
  "center": [
    0.0,
    0.0,
    0.0
  ],
  "id": "capsule_at",
  "kind": "CapsuleAt",
  "radius": 5.0,
  "slices": 16,
  "stacks": 16
}
```

### `Cone`

_(undocumented - add a doc comment to the variant in `feature.rs`.)_

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `height` | `Scalar` |

**Example**

```json
{
  "height": 10.0,
  "id": "cone",
  "kind": "Cone",
  "radius": 5.0
}
```

### `Cylinder`

Faceted (polyhedral) cylinder. `segments` is the number of sides; it is structural, not a measurement, so it stays a literal `usize`.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "height": 10.0,
  "id": "cylinder",
  "kind": "Cylinder",
  "radius": 5.0,
  "segments": 16
}
```

### `CylinderAt`

Cylinder at an axis-aligned position with chosen edge axis. The cylinder runs along `axis` ("x" | "y" | "z") starting from `base[axis_idx]` and extending by `height` in +axis direction. `base` is the center of the bottom cap (in world coordinates). Uses exact coordinate permutation for orientation to keep the boolean engine happy.

**Parameters**

| field | type |
|---|---|
| `base` | `[Scalar; 3]` |
| `axis` | `String` |
| `radius` | `Scalar` |
| `height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "axis": "z",
  "base": [
    0.0,
    0.0,
    0.0
  ],
  "height": 10.0,
  "id": "cylinder_at",
  "kind": "CylinderAt",
  "radius": 5.0,
  "segments": 16
}
```

### `Diamond`

Diamond: regular octahedron — 6 vertices, 8 triangular faces. Built as two pyramids joined base-to-base. `radius` is the distance from center to each of the 6 vertices.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |

**Example**

```json
{
  "id": "diamond",
  "kind": "Diamond",
  "radius": 5.0
}
```

### `Dome`

Hemisphere (dome): top half of a faceted sphere of `radius`, sitting on the xy plane. Built as `sphere_faceted - cutter_box` where the cutter clips everything below z=0.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `stacks` | `usize` |
| `slices` | `usize` |

**Example**

```json
{
  "id": "dome",
  "kind": "Dome",
  "radius": 5.0,
  "slices": 16,
  "stacks": 16
}
```

### `Donut`

Donut: faceted torus suitable for boolean composition. Unlike the analytic [`Feature::Torus`], its faces are planar quads, so it works with the kerf boolean engine. `major_segs` is the toroidal subdivision count (≥ 3), `minor_segs` the poloidal (≥ 3). Centered on the origin, axis along +z. `major_radius` must exceed `minor_radius` (otherwise the torus self-intersects).

**Parameters**

| field | type |
|---|---|
| `major_radius` | `Scalar` |
| `minor_radius` | `Scalar` |
| `major_segs` | `usize` |
| `minor_segs` | `usize` |

**Example**

```json
{
  "id": "donut",
  "kind": "Donut",
  "major_radius": 5.0,
  "major_segs": 16,
  "minor_radius": 1.0,
  "minor_segs": 8
}
```

### `Frustum`

_(undocumented - add a doc comment to the variant in `feature.rs`.)_

**Parameters**

| field | type |
|---|---|
| `top_radius` | `Scalar` |
| `bottom_radius` | `Scalar` |
| `height` | `Scalar` |

**Example**

```json
{
  "bottom_radius": 5.0,
  "height": 8.0,
  "id": "frustum",
  "kind": "Frustum",
  "top_radius": 3.0
}
```

### `Hemisphere`

Hemisphere: half of a faceted UV sphere — the +z half. Useful for domes, end caps, decorative bosses. `stacks` and `slices` are the UV subdivisions; the result has a planar circular base on z=0.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `stacks` | `usize` |
| `slices` | `usize` |

**Example**

```json
{
  "id": "hemisphere",
  "kind": "Hemisphere",
  "radius": 5.0,
  "slices": 16,
  "stacks": 16
}
```

### `Pyramid`

Right pyramid with a regular `n`-gon base of `radius` (circumradius) at z=0 and an apex at (0, 0, height). `segments` is the base sidedness (≥ 3). Uses the kerf `cone_faceted` primitive — fully planar, composes with booleans (the analytic `Cone` does not).

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "height": 10.0,
  "id": "pyramid",
  "kind": "Pyramid",
  "radius": 8.0,
  "segments": 6
}
```

### `RegularPrism`

Regular `n`-gon prism: base is a regular polygon of `segments` sides inscribed in a circle of `radius`, extruded along +z by `height`. At `segments = 4` the result is a square prism (rotated by π/4 of the n-gon phase, like cylinder_faceted's convention).

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "height": 10.0,
  "id": "regular_prism",
  "kind": "RegularPrism",
  "radius": 5.0,
  "segments": 16
}
```

### `Sphere`

_(undocumented - add a doc comment to the variant in `feature.rs`.)_

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |

**Example**

```json
{
  "id": "sphere",
  "kind": "Sphere",
  "radius": 5.0
}
```

### `SphereFaceted`

UV-style faceted sphere centered on the origin. Use this instead of the analytic `Sphere` whenever you want to compose with booleans (`Sphere`'s 1-face/0-edges topology breaks the engine). `stacks` is the number of latitude bands (≥ 2), `slices` is the longitude segments (≥ 3). Topology is V = 2 + (stacks-1) * slices.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `stacks` | `usize` |
| `slices` | `usize` |

**Example**

```json
{
  "id": "sphere_faceted",
  "kind": "SphereFaceted",
  "radius": 5.0,
  "slices": 16,
  "stacks": 16
}
```

### `SphericalCap`

Spherical cap: portion of a faceted sphere above a horizontal cutting plane at z = `radius - cap_height`. As `cap_height` approaches `2*radius` the cap approaches a full sphere; at `radius` it's a hemisphere. Built as `Sphere - HalfspaceBox`.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `cap_height` | `Scalar` |
| `stacks` | `usize` |
| `slices` | `usize` |

**Example**

```json
{
  "cap_height": 2.0,
  "id": "spherical_cap",
  "kind": "SphericalCap",
  "radius": 5.0,
  "slices": 16,
  "stacks": 16
}
```

### `Torus`

_(undocumented - add a doc comment to the variant in `feature.rs`.)_

**Parameters**

| field | type |
|---|---|
| `major_radius` | `Scalar` |
| `minor_radius` | `Scalar` |

**Example**

```json
{
  "id": "torus",
  "kind": "Torus",
  "major_radius": 5.0,
  "minor_radius": 1.0
}
```

### `TruncatedPyramid`

Truncated pyramid (frustum-shaped n-gon prism with two parallel regular polygons). Bottom n-gon of `bottom_radius` at z=0, top n-gon of `top_radius` at z=`height`. Both phase-shifted by π/n. `segments` is the polygon sidedness (≥ 3). Both radii must be > 0 (use `Pyramid` for a true apex).

**Parameters**

| field | type |
|---|---|
| `bottom_radius` | `Scalar` |
| `top_radius` | `Scalar` |
| `height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "bottom_radius": 8.0,
  "height": 10.0,
  "id": "truncated_pyramid",
  "kind": "TruncatedPyramid",
  "segments": 6,
  "top_radius": 4.0
}
```

### `Wedge`

Right-triangular prism: triangular cross-section with legs of length `width` (along x) and `height` (along z), extruded along `depth` (along y). Hypotenuse runs in the xz-plane from (width, 0, 0) to (0, 0, height).

**Parameters**

| field | type |
|---|---|
| `width` | `Scalar` |
| `depth` | `Scalar` |
| `height` | `Scalar` |

**Example**

```json
{
  "depth": 10.0,
  "height": 10.0,
  "id": "wedge",
  "kind": "Wedge",
  "width": 10.0
}
```

## Extrusions & profiles

### `ExtrudePolygon`

_(undocumented - add a doc comment to the variant in `feature.rs`.)_

**Parameters**

| field | type |
|---|---|
| `profile` | `Profile2D` |
| `direction` | `[Scalar; 3]` |

**Example**

```json
{
  "direction": [
    0.0,
    0.0,
    5.0
  ],
  "id": "extrude_polygon",
  "kind": "ExtrudePolygon",
  "profile": {
    "points": [
      [
        0.0,
        0.0
      ],
      [
        10.0,
        0.0
      ],
      [
        10.0,
        10.0
      ],
      [
        0.0,
        10.0
      ]
    ]
  }
}
```

### `Loft`

Loft (skin) between two parallel polygons of the same vertex count. `bottom` lies in the xy plane (z=0); `top` is the same number of (x, y) points lifted to z = `height`. Both must have the same length ≥ 3, both CCW from +z. Side faces are quads (will be non-planar if the two polygons differ in shape — the kernel stores those as "best-fit planar" with reduced boolean robustness).

**Parameters**

| field | type |
|---|---|
| `bottom` | `Profile2D` |
| `top` | `Profile2D` |
| `height` | `Scalar` |

**Example**

```json
{
  "bottom": {
    "points": [
      [
        0.0,
        0.0
      ],
      [
        10.0,
        0.0
      ],
      [
        10.0,
        10.0
      ],
      [
        0.0,
        10.0
      ]
    ]
  },
  "height": 10.0,
  "id": "loft",
  "kind": "Loft",
  "top": {
    "points": [
      [
        0.0,
        0.0
      ],
      [
        10.0,
        0.0
      ],
      [
        10.0,
        10.0
      ],
      [
        0.0,
        10.0
      ]
    ]
  }
}
```

### `Revolve`

Revolve an open polyline in the xz-plane around the z-axis. The first and last points must lie on the z-axis (x = 0); all interior points must have x > 0. Produces a closed axisymmetric solid.

**Parameters**

| field | type |
|---|---|
| `profile` | `Profile2D` |

**Example**

```json
{
  "id": "revolve",
  "kind": "Revolve",
  "profile": {
    "points": [
      [
        0.0,
        0.0
      ],
      [
        4.0,
        0.0
      ],
      [
        4.0,
        8.0
      ],
      [
        0.0,
        8.0
      ]
    ]
  }
}
```

### `TaperedExtrude`

Tapered extrude: extrude `profile` along +z by `height`, scaling the top profile by `top_scale` around the centroid. `top_scale` in (0, ∞); 1.0 is a normal prism, < 1 tapers inward (draft), > 1 tapers outward.

**Parameters**

| field | type |
|---|---|
| `profile` | `Profile2D` |
| `height` | `Scalar` |
| `top_scale` | `Scalar` |

**Example**

```json
{
  "height": 10.0,
  "id": "tapered_extrude",
  "kind": "TaperedExtrude",
  "profile": {
    "points": [
      [
        0.0,
        0.0
      ],
      [
        10.0,
        0.0
      ],
      [
        10.0,
        10.0
      ],
      [
        0.0,
        10.0
      ]
    ]
  },
  "top_scale": 0.7
}
```

## Sweeps & paths

### `Coil`

Coil: a helical chain of cylinder segments — useful for springs, thread roots, and any helical sweep. Built on top of SweepPath: the helix is sampled at `segments_per_turn * turns + 1` points and each pair of consecutive samples becomes a cylinder of `wire_radius`. Centered on the +z axis, starts at angle 0 in the +x direction.

**Parameters**

| field | type |
|---|---|
| `coil_radius` | `Scalar` |
| `wire_radius` | `Scalar` |
| `pitch` | `Scalar` |
| `turns` | `Scalar` |
| `segments_per_turn` | `usize` |
| `wire_segments` | `usize` |

**Example**

```json
{
  "coil_radius": 5.0,
  "id": "coil",
  "kind": "Coil",
  "pitch": 2.0,
  "segments_per_turn": 12,
  "turns": 4.0,
  "wire_radius": 0.5,
  "wire_segments": 8
}
```

### `PipeRun`

PipeRun: a chain of cylindrical pipe segments along a polyline of axis-aligned points. Each consecutive pair of points must differ in EXACTLY ONE coordinate (i.e., the segment is axis-aligned). At each turn (interior point) a hemispherical cap of `radius` joins the segments. Result is the union of all segments + interior hemispheres. Useful for plumbing layouts.

**Parameters**

| field | type |
|---|---|
| `points` | `Vec<[Scalar; 3]>` |
| `radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "pipe_run",
  "kind": "PipeRun",
  "points": [
    [
      0.0,
      0.0,
      0.0
    ],
    [
      10.0,
      0.0,
      0.0
    ],
    [
      10.0,
      10.0,
      0.0
    ]
  ],
  "radius": 5.0,
  "segments": 16
}
```

### `Spring`

Spring: a compressed-coil version of Coil. Identical parameter shape but with an `end_caps` flag — when true, attaches a small disk at each end of the coil for end-grinding the spring (a real spring detail). For now the disk is a small RegularPrism.

**Parameters**

| field | type |
|---|---|
| `coil_radius` | `Scalar` |
| `wire_radius` | `Scalar` |
| `pitch` | `Scalar` |
| `turns` | `Scalar` |
| `segments_per_turn` | `usize` |
| `wire_segments` | `usize` |

**Example**

```json
{
  "coil_radius": 5.0,
  "id": "spring",
  "kind": "Spring",
  "pitch": 2.0,
  "segments_per_turn": 12,
  "turns": 4.0,
  "wire_radius": 0.5,
  "wire_segments": 8
}
```

### `SweepPath`

SweepPath: like PipeRun but accepts arbitrary polylines (segments in ANY direction, not just axis-aligned). Each segment becomes a cylinder of `radius` whose axis runs along (p[i+1] - p[i]). Axis-aligned segments use the exact cyclic-permutation reorientation (no sin/cos noise); diagonal segments use a Rotation3 via face_towards, which introduces ~1e-15 floating-point noise. Single diagonals are robust; chained-diagonal differences are NOT — see the kernel's `cylinder_along_axis` rationale. Sharp miters between segments (no rounded joints).

**Parameters**

| field | type |
|---|---|
| `points` | `Vec<[Scalar; 3]>` |
| `radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "sweep_path",
  "kind": "SweepPath",
  "points": [
    [
      0.0,
      0.0,
      0.0
    ],
    [
      10.0,
      0.0,
      0.0
    ],
    [
      10.0,
      10.0,
      0.0
    ]
  ],
  "radius": 5.0,
  "segments": 16
}
```

## Booleans

### `Difference`

_(undocumented - add a doc comment to the variant in `feature.rs`.)_

**Parameters**

| field | type |
|---|---|
| `inputs` | `Vec<String>` |

**Example**

```json
{
  "id": "difference",
  "inputs": [
    "body",
    "tool"
  ],
  "kind": "Difference"
}
```

### `Intersection`

_(undocumented - add a doc comment to the variant in `feature.rs`.)_

**Parameters**

| field | type |
|---|---|
| `inputs` | `Vec<String>` |

**Example**

```json
{
  "id": "intersection",
  "inputs": [
    "body",
    "tool"
  ],
  "kind": "Intersection"
}
```

### `Union`

_(undocumented - add a doc comment to the variant in `feature.rs`.)_

**Parameters**

| field | type |
|---|---|
| `inputs` | `Vec<String>` |

**Example**

```json
{
  "id": "union",
  "inputs": [
    "body",
    "tool"
  ],
  "kind": "Union"
}
```

## Transforms & patterns

### `LinearPattern`

Replicate `input` `count` times along `offset`, unioning all copies. `count = 1` returns the input unchanged. `count = 0` is an error.

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `count` | `usize` |
| `offset` | `[Scalar; 3]` |

**Example**

```json
{
  "count": 3,
  "id": "linear_pattern",
  "input": "body",
  "kind": "LinearPattern",
  "offset": [
    12.0,
    0.0,
    0.0
  ]
}
```

### `Mirror`

Reflect `input` across the plane defined by `plane_origin` and `plane_normal`. Volume is preserved. The result is a single mirrored body (NOT unioned with the original — for a symmetric design, follow this with a Union of input + the mirrored result).

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `plane_origin` | `[Scalar; 3]` |
| `plane_normal` | `[Scalar; 3]` |

**Example**

```json
{
  "id": "mirror",
  "input": "body",
  "kind": "Mirror",
  "plane_normal": [
    1.0,
    0.0,
    0.0
  ],
  "plane_origin": [
    0.0,
    0.0,
    0.0
  ]
}
```

### `PolarPattern`

Replicate `input` `count` times around `axis` through `center`, distributing `total_angle_deg` evenly. `total_angle_deg = 360` gives a closed circle. Copies are unioned.

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `count` | `usize` |
| `axis` | `[Scalar; 3]` |
| `center` | `[Scalar; 3]` |
| `total_angle_deg` | `Scalar` |

**Example**

```json
{
  "axis": [
    0.0,
    0.0,
    1.0
  ],
  "center": [
    20.0,
    0.0,
    0.0
  ],
  "count": 4,
  "id": "polar_pattern",
  "input": "body",
  "kind": "PolarPattern",
  "total_angle_deg": 360.0
}
```

### `Rotate`

_(undocumented - add a doc comment to the variant in `feature.rs`.)_

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `axis` | `[Scalar; 3]` |
| `angle_deg` | `Scalar` |
| `center` | `[Scalar; 3]` |

**Example**

```json
{
  "angle_deg": 45.0,
  "axis": [
    0.0,
    0.0,
    1.0
  ],
  "center": [
    0.0,
    0.0,
    0.0
  ],
  "id": "rotate",
  "input": "body",
  "kind": "Rotate"
}
```

### `Scale`

Uniform scale of `input` around the origin. `factor` must be > 0. Volume scales as `factor³`.

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `factor` | `Scalar` |

**Example**

```json
{
  "factor": 1.5,
  "id": "scale",
  "input": "body",
  "kind": "Scale"
}
```

### `ScaleXYZ`

Non-uniform scale: independent factors per axis. Only valid when `input` is a fully-faceted solid (all faces planar, all edges line segments). Faceted primitives like `Box`, `Cylinder`, `SphereFaceted`, `Pyramid`, `RegularPrism`, `LBracket`, `IBeam`, etc. are valid inputs; analytic primitives like `Sphere`, `Cone`, `Frustum`, `Torus`, `Cylinder` (raw) are not. Volume scales as `sx * sy * sz`.

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `factors` | `[Scalar; 3]` |

**Example**

```json
{
  "factors": [
    1.5,
    1.0,
    0.8
  ],
  "id": "scale_x_y_z",
  "input": "body",
  "kind": "ScaleXYZ"
}
```

### `Translate`

_(undocumented - add a doc comment to the variant in `feature.rs`.)_

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `offset` | `[Scalar; 3]` |

**Example**

```json
{
  "id": "translate",
  "input": "body",
  "kind": "Translate",
  "offset": [
    5.0,
    0.0,
    0.0
  ]
}
```

## Edge treatments (fillet/chamfer/cut)

### `Chamfer`

Bevel an axis-aligned 90° edge of `input` by a 45° flat cut of `setback` (distance from the edge that the cut starts on each face).

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `axis` | `String` |
| `edge_min` | `[Scalar; 3]` |
| `edge_length` | `Scalar` |
| `setback` | `Scalar` |
| `quadrant` | `String` |

**Example**

```json
{
  "axis": "z",
  "edge_length": 10.0,
  "edge_min": [
    10.0,
    10.0,
    0.0
  ],
  "id": "chamfer",
  "input": "body",
  "kind": "Chamfer",
  "quadrant": "nn",
  "setback": 1.0
}
```

### `CornerCut`

Subtract an axis-aligned box (a "corner cutter") from `input`. The cutter has its `min` corner at `corner` and the given `extents`. Useful as quick-and-dirty chamfer-by-cut or "flatten this corner of a bounding box". Composes as `Difference(input, BoxAt(extents, corner))`.

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `corner` | `[Scalar; 3]` |
| `extents` | `[Scalar; 3]` |

**Example**

```json
{
  "corner": [
    7.0,
    7.0,
    7.0
  ],
  "extents": [
    5.0,
    5.0,
    5.0
  ],
  "id": "corner_cut",
  "input": "body",
  "kind": "CornerCut"
}
```

### `Fillet`

Round an axis-aligned 90° edge of `input` with a quarter-circle of radius `radius`.

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `axis` | `String` |
| `edge_min` | `[Scalar; 3]` |
| `edge_length` | `Scalar` |
| `radius` | `Scalar` |
| `quadrant` | `String` |
| `segments` | `usize` |

**Example**

```json
{
  "axis": "z",
  "edge_length": 10.0,
  "edge_min": [
    10.0,
    10.0,
    0.0
  ],
  "id": "fillet",
  "input": "body",
  "kind": "Fillet",
  "quadrant": "nn",
  "radius": 2.0,
  "segments": 8
}
```

### `Fillets`

Multiple `Fillet`s applied to the same `input` in one operation. Builds each fillet's wedge cutter relative to the unmodified input, unions the wedges, and subtracts the composite cutter once. This avoids the "second fillet meets first fillet's curved face" failure that single-`Fillet` stacking has.

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `edges` | `Vec<FilletEdge>` |

**Example**

```json
{
  "edges": [
    {
      "axis": "z",
      "edge_length": 10.0,
      "edge_min": [
        10.0,
        10.0,
        0.0
      ],
      "quadrant": "nn",
      "radius": 1.5,
      "segments": 8
    }
  ],
  "id": "fillets",
  "input": "body",
  "kind": "Fillets"
}
```

## Holes & pockets

### `BoltCircle`

Subtract a polar array of through-holes around `center`. Each hole is at `bolt_circle_radius` from center, distributed evenly around `axis` ("x"|"y"|"z"). `count` ≥ 1 holes, `radius` each, length `depth` along -axis from `center` (which sits on the +axis-facing opening surface).

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `axis` | `String` |
| `center` | `[Scalar; 3]` |
| `bolt_circle_radius` | `Scalar` |
| `count` | `usize` |
| `radius` | `Scalar` |
| `depth` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "axis": "z",
  "bolt_circle_radius": 3.0,
  "center": [
    5.0,
    5.0,
    10.0
  ],
  "count": 4,
  "depth": 8.0,
  "id": "bolt_circle",
  "input": "body",
  "kind": "BoltCircle",
  "radius": 0.5,
  "segments": 12
}
```

### `Counterbore`

Counterbore: subtract a stepped hole from `input`. The "drill" portion has `drill_radius` and runs full `total_depth`. The "counterbore" portion is a wider cylinder of `cbore_radius` and `cbore_depth`, sitting at the top (the +axis end of the hole). `axis` is "x"|"y"|"z" — the drill direction is -axis (the hole goes INTO the body from the +axis-facing surface). `top_center` is the center of the counterbore opening on that surface.

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `axis` | `String` |
| `top_center` | `[Scalar; 3]` |
| `drill_radius` | `Scalar` |
| `cbore_radius` | `Scalar` |
| `cbore_depth` | `Scalar` |
| `total_depth` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "axis": "z",
  "cbore_depth": 2.0,
  "cbore_radius": 3.0,
  "drill_radius": 1.5,
  "id": "counterbore",
  "input": "body",
  "kind": "Counterbore",
  "segments": 16,
  "top_center": [
    5.0,
    5.0,
    10.0
  ],
  "total_depth": 8.0
}
```

### `Countersink`

Countersink: subtract a hole with a conical chamfer at the top (the +axis-facing surface of the body). The straight drill portion has `drill_radius` and runs the full `total_depth`. The conical portion expands from `drill_radius` at depth `csink_depth` to `csink_radius` at the top surface (depth 0). `axis` chooses the drill direction: the hole goes INTO the body in -axis from the `top_center` opening.

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `axis` | `String` |
| `top_center` | `[Scalar; 3]` |
| `drill_radius` | `Scalar` |
| `csink_radius` | `Scalar` |
| `csink_depth` | `Scalar` |
| `total_depth` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "axis": "z",
  "csink_depth": 1.5,
  "csink_radius": 3.0,
  "drill_radius": 1.5,
  "id": "countersink",
  "input": "body",
  "kind": "Countersink",
  "segments": 16,
  "top_center": [
    5.0,
    5.0,
    10.0
  ],
  "total_depth": 8.0
}
```

### `DovetailSlot`

Trapezoidal slot (dovetail) cross-section extruded along +y by `length`. The trapezoid's `top_width` (wider, at z=`depth`) and `bottom_width` (narrower, at z=0) sit on x ∈ [-W/2, +W/2]; the slot extends in z from 0 to `depth`. Typical dovetail uses `top_width > bottom_width`. Use as a cutter via Difference.

**Parameters**

| field | type |
|---|---|
| `bottom_width` | `Scalar` |
| `top_width` | `Scalar` |
| `depth` | `Scalar` |
| `length` | `Scalar` |

**Example**

```json
{
  "bottom_width": 10.0,
  "depth": 10.0,
  "id": "dovetail_slot",
  "kind": "DovetailSlot",
  "length": 10.0,
  "top_width": 10.0
}
```

### `DrawerSlot`

DrawerSlot: a rectangular slot for a sliding drawer — open at the front, closed on three sides (back, left, right). `width` is the slot opening, `depth` along +y, `height` along +z, walls of `wall_thickness`.

**Parameters**

| field | type |
|---|---|
| `width` | `Scalar` |
| `depth` | `Scalar` |
| `height` | `Scalar` |
| `wall_thickness` | `Scalar` |

**Example**

```json
{
  "depth": 8.0,
  "height": 4.0,
  "id": "drawer_slot",
  "kind": "DrawerSlot",
  "wall_thickness": 0.8,
  "width": 10.0
}
```

### `FilletedSlot`

FilletedSlot: a stadium-shaped slot but with rounded corners (ie a fully rounded rectangle, parameterized by its own corner radius rather than the slot end semicircle radius). When `corner_radius == width / 2`, this is identical to Slot3D.

**Parameters**

| field | type |
|---|---|
| `length` | `Scalar` |
| `width` | `Scalar` |
| `corner_radius` | `Scalar` |
| `depth` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "corner_radius": 2.0,
  "depth": 10.0,
  "id": "filleted_slot",
  "kind": "FilletedSlot",
  "length": 10.0,
  "segments": 16,
  "width": 10.0
}
```

### `HexHole`

Subtract a hex-shaped through-pocket. `top_center` is the center of the opening on the +axis-facing surface. `inscribed_radius` (apothem) sets the hex size — distance from center to flat side. `depth` is the pocket length along -axis.

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `axis` | `String` |
| `top_center` | `[Scalar; 3]` |
| `inscribed_radius` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "axis": "z",
  "depth": 8.0,
  "id": "hex_hole",
  "input": "body",
  "inscribed_radius": 2.0,
  "kind": "HexHole",
  "top_center": [
    5.0,
    5.0,
    10.0
  ]
}
```

### `HoleArray`

Subtract a linear array of through-holes from `input`. The first hole is at `start` (center of the +axis-facing opening); successive holes are spaced by `offset` (3D vector). `count` ≥ 1. Each hole is a cylinder of `radius`, axis along `axis`, length equal to `depth` extending in -axis direction (with overhang).

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `axis` | `String` |
| `start` | `[Scalar; 3]` |
| `offset` | `[Scalar; 3]` |
| `count` | `usize` |
| `radius` | `Scalar` |
| `depth` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "axis": "z",
  "count": 2,
  "depth": 8.0,
  "id": "hole_array",
  "input": "body",
  "kind": "HoleArray",
  "offset": [
    2.5,
    0.0,
    0.0
  ],
  "radius": 0.6,
  "segments": 12,
  "start": [
    3.0,
    5.0,
    10.0
  ]
}
```

### `Keyway`

Keyway: a rectangular slot extruded along +z that pairs with a rectangular key on a shaft. Centered on x at y=0; extends from y=0 to y=`depth_into`. Useful for mating with shafts.

**Parameters**

| field | type |
|---|---|
| `width` | `Scalar` |
| `depth_into` | `Scalar` |
| `length` | `Scalar` |

**Example**

```json
{
  "depth_into": 3.0,
  "id": "keyway",
  "kind": "Keyway",
  "length": 10.0,
  "width": 10.0
}
```

### `Mortise`

Mortise: rectangular pocket cut into the +z face of a workpiece. Centered on (cx, cy), spanning (width, length, depth) where depth is along -z. Produces a freestanding pocket solid; subtract it from your workpiece via Difference.

**Parameters**

| field | type |
|---|---|
| `center` | `[Scalar; 2]` |
| `width` | `Scalar` |
| `length` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "center": [
    0.0,
    0.0
  ],
  "depth": 10.0,
  "id": "mortise",
  "kind": "Mortise",
  "length": 10.0,
  "width": 10.0
}
```

### `PrismHole`

PrismHole: an n-sided regular prism with a central cylindrical hole through it.

**Parameters**

| field | type |
|---|---|
| `sides` | `usize` |
| `outer_radius` | `Scalar` |
| `hole_radius` | `Scalar` |
| `height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "height": 10.0,
  "hole_radius": 1.5,
  "id": "prism_hole",
  "kind": "PrismHole",
  "outer_radius": 8.0,
  "segments": 16,
  "sides": 4
}
```

### `Slot`

Stadium-shaped slot: a rounded-rectangle profile extruded along `direction`. The slot has axis-aligned (in the local profile frame) straight sides of `length` connecting two semicircles of `radius`. Currently fixed to lie in the xy plane, extruded in +z.

**Parameters**

| field | type |
|---|---|
| `length` | `Scalar` |
| `radius` | `Scalar` |
| `height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "height": 10.0,
  "id": "slot",
  "kind": "Slot",
  "length": 10.0,
  "radius": 5.0,
  "segments": 16
}
```

### `Slot3D`

Slot3D: a stadium-shaped slot — rectangle with semicircular ends. `length` along +x is the long axis (between center of each end semicircle); `width` is the short axis (and equals the diameter of each end). Extruded along +z by `depth`.

**Parameters**

| field | type |
|---|---|
| `length` | `Scalar` |
| `width` | `Scalar` |
| `depth` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "depth": 10.0,
  "id": "slot3_d",
  "kind": "Slot3D",
  "length": 10.0,
  "segments": 16,
  "width": 10.0
}
```

### `SquareHole`

Subtract a square through-pocket. `top_center` on the +axis surface, `side` length, `depth` along -axis.

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `axis` | `String` |
| `top_center` | `[Scalar; 3]` |
| `side` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "axis": "z",
  "depth": 8.0,
  "id": "square_hole",
  "input": "body",
  "kind": "SquareHole",
  "side": 3.0,
  "top_center": [
    5.0,
    5.0,
    10.0
  ]
}
```

### `TSlot`

T-slot machining feature: a rectangular slot with a wider rectangular base, used for clamping fixtures. Cross-section is like an inverted T. Extruded along +z by `depth`. Centered on x.

**Parameters**

| field | type |
|---|---|
| `slot_width` | `Scalar` |
| `slot_height` | `Scalar` |
| `base_width` | `Scalar` |
| `base_height` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "base_height": 2.0,
  "base_width": 8.0,
  "depth": 10.0,
  "id": "t_slot",
  "kind": "TSlot",
  "slot_height": 4.0,
  "slot_width": 3.0
}
```

### `VeeGroove`

V-groove cutter: triangular cross-section extruded along +y by `length`. The notch opens at z=`depth` with width `top_width`, converges to a sharp edge at z=0 (a single point along x=0). Use as a cutter via Difference.

**Parameters**

| field | type |
|---|---|
| `top_width` | `Scalar` |
| `depth` | `Scalar` |
| `length` | `Scalar` |

**Example**

```json
{
  "depth": 10.0,
  "id": "vee_groove",
  "kind": "VeeGroove",
  "length": 10.0,
  "top_width": 10.0
}
```

## Fasteners (bolts, screws, rivets, etc.)

### `Bolt`

Hex-head bolt: hexagonal head + cylindrical shaft, both along +z. The head is a `RegularPrism { sides: 6 }` of `head_inscribed_radius` (apothem) and `head_thickness`, sitting at z ∈ [0, head_thickness]. The shaft is a Cylinder of `shaft_radius` and `shaft_length`, sitting at z ∈ [head_thickness, head_thickness + shaft_length]. Result is a single solid: head ∪ shaft.

**Parameters**

| field | type |
|---|---|
| `head_inscribed_radius` | `Scalar` |
| `head_thickness` | `Scalar` |
| `shaft_radius` | `Scalar` |
| `shaft_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "head_inscribed_radius": 3.0,
  "head_thickness": 2.0,
  "id": "bolt",
  "kind": "Bolt",
  "segments": 16,
  "shaft_length": 8.0,
  "shaft_radius": 1.5
}
```

### `CapBolt`

CapBolt: a wider-headed version of Bolt with a concave cap. Shape: a cylinder for the head + a cylinder for the shaft. Ratio: head_radius is significantly larger than shaft_radius.

**Parameters**

| field | type |
|---|---|
| `head_radius` | `Scalar` |
| `head_height` | `Scalar` |
| `shaft_radius` | `Scalar` |
| `shaft_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "head_height": 3.0,
  "head_radius": 4.0,
  "id": "cap_bolt",
  "kind": "CapBolt",
  "segments": 16,
  "shaft_length": 10.0,
  "shaft_radius": 2.0
}
```

### `CapScrew`

Socket-head cap screw (SHCS): cylindrical head + cylindrical shaft. Head is at z ∈ [0, head_thickness] with radius `head_radius`; shaft is at z ∈ [head_thickness, head_thickness + shaft_length] with `shaft_radius`. Result is head ∪ shaft.

**Parameters**

| field | type |
|---|---|
| `head_radius` | `Scalar` |
| `head_thickness` | `Scalar` |
| `shaft_radius` | `Scalar` |
| `shaft_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "head_radius": 4.0,
  "head_thickness": 3.0,
  "id": "cap_screw",
  "kind": "CapScrew",
  "segments": 16,
  "shaft_length": 10.0,
  "shaft_radius": 2.0
}
```

### `EyeBolt`

EyeBolt: a bolt with a ring (eye) instead of a flat head. The ring is approximated as a faceted donut (torus_faceted) joined to a cylindrical shaft.

**Parameters**

| field | type |
|---|---|
| `ring_major_radius` | `Scalar` |
| `ring_minor_radius` | `Scalar` |
| `shaft_radius` | `Scalar` |
| `shaft_length` | `Scalar` |
| `ring_segs` | `usize` |
| `shaft_segments` | `usize` |

**Example**

```json
{
  "id": "eye_bolt",
  "kind": "EyeBolt",
  "ring_major_radius": 4.0,
  "ring_minor_radius": 1.0,
  "ring_segs": 16,
  "shaft_length": 10.0,
  "shaft_radius": 2.0,
  "shaft_segments": 16
}
```

### `FlangedBolt`

FlangedBolt: a bolt with an integrated wider washer-like flange under the head. Three cylinders stacked: head, flange, shaft.

**Parameters**

| field | type |
|---|---|
| `head_radius` | `Scalar` |
| `head_height` | `Scalar` |
| `flange_radius` | `Scalar` |
| `flange_thickness` | `Scalar` |
| `shaft_radius` | `Scalar` |
| `shaft_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "flange_radius": 7.0,
  "flange_thickness": 1.0,
  "head_height": 3.0,
  "head_radius": 4.0,
  "id": "flanged_bolt",
  "kind": "FlangedBolt",
  "segments": 16,
  "shaft_length": 10.0,
  "shaft_radius": 2.0
}
```

### `FlatHeadScrew`

FlatHeadScrew: a countersunk flat-head screw — frustum head (cone-shaped) tapering from `head_radius` at the top to `shaft_radius` at the join, then cylindrical shaft below.

**Parameters**

| field | type |
|---|---|
| `head_radius` | `Scalar` |
| `head_height` | `Scalar` |
| `shaft_radius` | `Scalar` |
| `shaft_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "head_height": 3.0,
  "head_radius": 4.0,
  "id": "flat_head_screw",
  "kind": "FlatHeadScrew",
  "segments": 16,
  "shaft_length": 10.0,
  "shaft_radius": 2.0
}
```

### `FlatWasher`

FlatWasher: a thin annulus — same as CircularRing with explicit "washer" intent. Catalog companion to Washer (already in the fastener bucket).

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `inner_radius` | `Scalar` |
| `thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "flat_washer",
  "inner_radius": 2.0,
  "kind": "FlatWasher",
  "outer_radius": 4.0,
  "segments": 16,
  "thickness": 0.5
}
```

### `Nut`

Hex nut: hexagonal prism (apothem `inscribed_radius`, `thickness`) with a centered through-hole of `bore_radius`. Sits at z ∈ [0, thickness].

**Parameters**

| field | type |
|---|---|
| `inscribed_radius` | `Scalar` |
| `bore_radius` | `Scalar` |
| `thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "bore_radius": 1.5,
  "id": "nut",
  "inscribed_radius": 3.0,
  "kind": "Nut",
  "segments": 6,
  "thickness": 2.0
}
```

### `Rivet`

Rivet: cylindrical body + dome head. The dome is approximated by a frustum (small radius at top, larger at the body join) to avoid sphere boolean limitations.

**Parameters**

| field | type |
|---|---|
| `body_radius` | `Scalar` |
| `body_length` | `Scalar` |
| `head_radius` | `Scalar` |
| `head_height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "body_length": 8.0,
  "body_radius": 2.0,
  "head_height": 1.0,
  "head_radius": 3.0,
  "id": "rivet",
  "kind": "Rivet",
  "segments": 16
}
```

### `ShoulderBolt`

ShoulderBolt: a bolt with two diameters — a wider "shoulder" near the head and a narrower threaded portion below. Useful for precise positioning bolts.

**Parameters**

| field | type |
|---|---|
| `head_radius` | `Scalar` |
| `head_height` | `Scalar` |
| `shoulder_radius` | `Scalar` |
| `shoulder_length` | `Scalar` |
| `thread_radius` | `Scalar` |
| `thread_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "head_height": 3.0,
  "head_radius": 4.0,
  "id": "shoulder_bolt",
  "kind": "ShoulderBolt",
  "segments": 16,
  "shoulder_length": 10.0,
  "shoulder_radius": 2.5,
  "thread_length": 10.0,
  "thread_radius": 1.5
}
```

### `SocketHeadCapScrew`

SocketHeadCapScrew: a hex socket head + cylindrical shaft. Head is a hex prism (6-sided, fits a hex key); shaft is a cylinder.

**Parameters**

| field | type |
|---|---|
| `head_radius` | `Scalar` |
| `head_height` | `Scalar` |
| `shaft_radius` | `Scalar` |
| `shaft_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "head_height": 3.0,
  "head_radius": 4.0,
  "id": "socket_head_cap_screw",
  "kind": "SocketHeadCapScrew",
  "segments": 16,
  "shaft_length": 10.0,
  "shaft_radius": 2.0
}
```

### `ThreadInsert`

ThreadInsert: a tubular insert with a hex outer (so it can be driven into the workpiece) and a smooth inner bore. Used for strengthening threads in soft materials.

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `inner_radius` | `Scalar` |
| `length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "thread_insert",
  "inner_radius": 1.5,
  "kind": "ThreadInsert",
  "length": 6.0,
  "outer_radius": 2.5,
  "segments": 16
}
```

### `Washer`

Flat washer: outer cylinder of `outer_radius` and `thickness` minus a centered bore of `inner_radius`. Sits at z ∈ [0, thickness].

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `inner_radius` | `Scalar` |
| `thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "washer",
  "inner_radius": 2.0,
  "kind": "Washer",
  "outer_radius": 4.0,
  "segments": 16,
  "thickness": 0.5
}
```

### `WingedScrew`

WingedScrew: a screw with two flat "wings" on either side of the head for hand-tightening. Cylinder shaft + center hex head + two rectangular wing tabs.

**Parameters**

| field | type |
|---|---|
| `shaft_radius` | `Scalar` |
| `shaft_length` | `Scalar` |
| `head_radius` | `Scalar` |
| `head_height` | `Scalar` |
| `wing_length` | `Scalar` |
| `wing_thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "head_height": 3.0,
  "head_radius": 4.0,
  "id": "winged_screw",
  "kind": "WingedScrew",
  "segments": 16,
  "shaft_length": 10.0,
  "shaft_radius": 2.0,
  "wing_length": 1.0,
  "wing_thickness": 1.0
}
```

## Joinery (mortise/tenon/dovetail)

### `CrossKey`

CrossKey: a + shaped key for keyed shaft-hub connections. Two perpendicular rectangular bars meeting at the center.

**Parameters**

| field | type |
|---|---|
| `bar_length` | `Scalar` |
| `bar_thickness` | `Scalar` |
| `bar_height` | `Scalar` |

**Example**

```json
{
  "bar_height": 1.0,
  "bar_length": 10.0,
  "bar_thickness": 1.0,
  "id": "cross_key",
  "kind": "CrossKey"
}
```

### `DovetailRail`

DovetailRail: like DovetailSlot but extruded over a long length — used as a sliding mate. `width` is the wider rail bottom, `top_width` the narrower top, `height` the dovetail depth, and `length` the extrude distance along +z.

**Parameters**

| field | type |
|---|---|
| `width` | `Scalar` |
| `top_width` | `Scalar` |
| `height` | `Scalar` |
| `length` | `Scalar` |

**Example**

```json
{
  "height": 4.0,
  "id": "dovetail_rail",
  "kind": "DovetailRail",
  "length": 20.0,
  "top_width": 6.0,
  "width": 10.0
}
```

### `FingerJoint`

FingerJoint: a row of `count` rectangular fingers extending in +z from a base plate. Each finger is `finger_width` wide, `finger_height` tall (in z), with `gap_width` between adjacent fingers. Total row length = count * finger_width + (count-1) * gap_width. Useful for box joints in woodworking.

**Parameters**

| field | type |
|---|---|
| `count` | `usize` |
| `finger_width` | `Scalar` |
| `finger_height` | `Scalar` |
| `finger_depth` | `Scalar` |
| `gap_width` | `Scalar` |

**Example**

```json
{
  "count": 3,
  "finger_depth": 2.0,
  "finger_height": 3.0,
  "finger_width": 2.0,
  "gap_width": 2.0,
  "id": "finger_joint",
  "kind": "FingerJoint"
}
```

### `SquareKey`

SquareKey: a square cross-section key — extruded along +z by `length`. Used for keyed shaft connections.

**Parameters**

| field | type |
|---|---|
| `side` | `Scalar` |
| `length` | `Scalar` |

**Example**

```json
{
  "id": "square_key",
  "kind": "SquareKey",
  "length": 10.0,
  "side": 10.0
}
```

### `Tenon`

Tenon: rectangular tongue extending from the +z face of a workpiece. Same parameter shape as Mortise; produces the freestanding tongue solid for unioning onto the workpiece.

**Parameters**

| field | type |
|---|---|
| `center` | `[Scalar; 2]` |
| `width` | `Scalar` |
| `length` | `Scalar` |
| `height` | `Scalar` |

**Example**

```json
{
  "center": [
    0.0,
    0.0
  ],
  "height": 10.0,
  "id": "tenon",
  "kind": "Tenon",
  "length": 10.0,
  "width": 10.0
}
```

## Structural shapes (beams, channels, brackets)

### `AngleIron`

Angle iron (equal-leg L-section) — like LBracket but with both legs the same length, and chosen extrusion axis (so it works as a frame member regardless of orientation). For now the extrusion runs along +z only.

**Parameters**

| field | type |
|---|---|
| `leg_length` | `Scalar` |
| `thickness` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "depth": 10.0,
  "id": "angle_iron",
  "kind": "AngleIron",
  "leg_length": 10.0,
  "thickness": 1.0
}
```

### `ArchedDoorway`

ArchedDoorway: rectangular base with a half-circle arch on top, hollow inside (so it's a door-shaped frame). Useful for gateways, archways. Wall thickness is uniform.

**Parameters**

| field | type |
|---|---|
| `width` | `Scalar` |
| `height` | `Scalar` |
| `wall_thickness` | `Scalar` |
| `depth` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "depth": 10.0,
  "height": 10.0,
  "id": "arched_doorway",
  "kind": "ArchedDoorway",
  "segments": 16,
  "wall_thickness": 1.0,
  "width": 10.0
}
```

### `AsymmetricBracket`

AsymmetricBracket: an L-bracket where the two legs have different lengths. Like LBracket but `width_a` and `width_b` can differ.

**Parameters**

| field | type |
|---|---|
| `leg_a_length` | `Scalar` |
| `leg_b_length` | `Scalar` |
| `thickness` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "depth": 6.0,
  "id": "asymmetric_bracket",
  "kind": "AsymmetricBracket",
  "leg_a_length": 10.0,
  "leg_b_length": 8.0,
  "thickness": 1.0
}
```

### `AxleShaft`

AxleShaft: a long cylinder with two reduced-diameter ends (the "necks" that fit into bearings). Useful for shafts. The middle section has `body_radius` and length `body_length`; each neck has `neck_radius` and length `neck_length`.

**Parameters**

| field | type |
|---|---|
| `body_radius` | `Scalar` |
| `body_length` | `Scalar` |
| `neck_radius` | `Scalar` |
| `neck_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "body_length": 10.0,
  "body_radius": 5.0,
  "id": "axle_shaft",
  "kind": "AxleShaft",
  "neck_length": 10.0,
  "neck_radius": 2.0,
  "segments": 16
}
```

### `BasePlate`

BasePlate: a thick rectangular base with optional rounded corners. Like RoundedRect but with explicit "base" intent. `corner_radius` of 0 = sharp corners.

**Parameters**

| field | type |
|---|---|
| `width` | `Scalar` |
| `height` | `Scalar` |
| `thickness` | `Scalar` |
| `corner_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "corner_radius": 2.0,
  "height": 10.0,
  "id": "base_plate",
  "kind": "BasePlate",
  "segments": 16,
  "thickness": 1.0,
  "width": 10.0
}
```

### `BeamWithHoles`

BeamWithHoles: a rectangular beam with a row of evenly spaced circular through-holes. Used for trusses, lifting bars.

**Parameters**

| field | type |
|---|---|
| `length` | `Scalar` |
| `width` | `Scalar` |
| `height` | `Scalar` |
| `hole_radius` | `Scalar` |
| `hole_count` | `usize` |
| `segments` | `usize` |

**Example**

```json
{
  "height": 10.0,
  "hole_count": 4,
  "hole_radius": 1.5,
  "id": "beam_with_holes",
  "kind": "BeamWithHoles",
  "length": 10.0,
  "segments": 16,
  "width": 10.0
}
```

### `BeltLoop`

BeltLoop: a small thin rectangular loop — like a belt buckle frame. `outer_width` × `outer_height` rectangle minus inner rectangle of (`outer_width-2t`) × (`outer_height-2t`), extruded along +z by `depth`.

**Parameters**

| field | type |
|---|---|
| `outer_width` | `Scalar` |
| `outer_height` | `Scalar` |
| `wall_thickness` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "depth": 10.0,
  "id": "belt_loop",
  "kind": "BeltLoop",
  "outer_height": 10.0,
  "outer_width": 10.0,
  "wall_thickness": 1.0
}
```

### `CChannel`

C-channel (a U-channel rotated 90° so it opens to +x). Same parameter set as UChannel: outer width, height, wall thickness, and extrusion depth along +z.

**Parameters**

| field | type |
|---|---|
| `width` | `Scalar` |
| `height` | `Scalar` |
| `thickness` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "depth": 10.0,
  "height": 10.0,
  "id": "c_channel",
  "kind": "CChannel",
  "thickness": 1.0,
  "width": 10.0
}
```

### `ChainLink`

ChainLink: an oval ring — a stadium-shaped outer profile minus a stadium-shaped inner profile. `length` and `width` are the outer dimensions, `wall_thickness` the wall around the inner stadium hole. Extruded along +z by `depth`.

**Parameters**

| field | type |
|---|---|
| `length` | `Scalar` |
| `width` | `Scalar` |
| `wall_thickness` | `Scalar` |
| `depth` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "depth": 10.0,
  "id": "chain_link",
  "kind": "ChainLink",
  "length": 10.0,
  "segments": 16,
  "wall_thickness": 1.0,
  "width": 10.0
}
```

### `ChamferedPlate`

ChamferedPlate: rectangular plate where all 4 corners are chamfered (cut at 45°) instead of rounded. `chamfer` is the distance from each corner the chamfer cuts.

**Parameters**

| field | type |
|---|---|
| `width` | `Scalar` |
| `height` | `Scalar` |
| `thickness` | `Scalar` |
| `chamfer` | `Scalar` |

**Example**

```json
{
  "chamfer": 1.0,
  "height": 10.0,
  "id": "chamfered_plate",
  "kind": "ChamferedPlate",
  "thickness": 1.0,
  "width": 10.0
}
```

### `Cleat`

Cleat: T-shape wall mount — a horizontal arm and a vertical support, both rectangular. `arm_length` along +x, `arm_height` along +z, `support_width` along +x, `support_height` along +z, `thickness` along +y. The two are joined L-style at the origin.

**Parameters**

| field | type |
|---|---|
| `arm_length` | `Scalar` |
| `arm_height` | `Scalar` |
| `support_width` | `Scalar` |
| `support_height` | `Scalar` |
| `thickness` | `Scalar` |

**Example**

```json
{
  "arm_height": 10.0,
  "arm_length": 10.0,
  "id": "cleat",
  "kind": "Cleat",
  "support_height": 10.0,
  "support_width": 2.0,
  "thickness": 1.0
}
```

### `CornerBracket`

CornerBracket: a 3-leg corner support — three perpendicular rectangular plates meeting at a corner. Useful for box reinforcement.

**Parameters**

| field | type |
|---|---|
| `leg_x` | `Scalar` |
| `leg_y` | `Scalar` |
| `leg_z` | `Scalar` |
| `thickness` | `Scalar` |

**Example**

```json
{
  "id": "corner_bracket",
  "kind": "CornerBracket",
  "leg_x": 6.0,
  "leg_y": 6.0,
  "leg_z": 6.0,
  "thickness": 1.0
}
```

### `Cross`

Cross: a 4-way pipe junction. Four cylinders meeting at the origin, all the same radius. Two pairs of antiparallel legs.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `leg_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "cross",
  "kind": "Cross",
  "leg_length": 10.0,
  "radius": 5.0,
  "segments": 16
}
```

### `CrossBrace`

CrossBrace: an X-shaped support — two diagonal rectangular bars crossed in a square frame. Useful for truss panels.

**Parameters**

| field | type |
|---|---|
| `frame_size` | `Scalar` |
| `bar_thickness` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "bar_thickness": 1.0,
  "depth": 10.0,
  "frame_size": 10.0,
  "id": "cross_brace",
  "kind": "CrossBrace"
}
```

### `CylinderShellAt`

CylinderShellAt: a hollow cylinder positioned at an arbitrary origin along an arbitrary axis (x/y/z). Same params as Tube but with named axis like CylinderAt.

**Parameters**

| field | type |
|---|---|
| `base` | `[Scalar; 3]` |
| `axis` | `String` |
| `outer_radius` | `Scalar` |
| `inner_radius` | `Scalar` |
| `length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "axis": "z",
  "base": [
    0.0,
    0.0,
    0.0
  ],
  "id": "cylinder_shell_at",
  "inner_radius": 4.0,
  "kind": "CylinderShellAt",
  "length": 10.0,
  "outer_radius": 8.0,
  "segments": 16
}
```

### `Elbow90`

Elbow90: two perpendicular cylinders joined at right angle (an L-shaped pipe section). Each leg has the same radius. The first leg runs along +x for `leg_length`, the second from the corner along +y for the same length.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `leg_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "elbow90",
  "kind": "Elbow90",
  "leg_length": 10.0,
  "radius": 5.0,
  "segments": 16
}
```

### `EndCap`

EndCap: a cylindrical cap that fits over a tube — outer cylinder + closed top + inner cavity that fits the tube. Like Cup but inverted (open at the bottom).

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `height` | `Scalar` |
| `wall_thickness` | `Scalar` |
| `cap_thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "cap_thickness": 1.0,
  "height": 10.0,
  "id": "end_cap",
  "kind": "EndCap",
  "outer_radius": 8.0,
  "segments": 16,
  "wall_thickness": 1.0
}
```

### `FerruleEnd`

FerruleEnd: a frustum-shaped tube end fitting (one end larger than the other, like a flared pipe end).

**Parameters**

| field | type |
|---|---|
| `small_radius` | `Scalar` |
| `large_radius` | `Scalar` |
| `wall_thickness` | `Scalar` |
| `length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "ferrule_end",
  "kind": "FerruleEnd",
  "large_radius": 5.0,
  "length": 8.0,
  "segments": 16,
  "small_radius": 3.0,
  "wall_thickness": 0.5
}
```

### `FunnelTube`

FunnelTube: a hollow funnel — outer cone shell with a constant `wall_thickness`. Like ReducerCone but easier to specify (top and bottom outer radii + wall thickness).

**Parameters**

| field | type |
|---|---|
| `top_outer_radius` | `Scalar` |
| `bottom_outer_radius` | `Scalar` |
| `wall_thickness` | `Scalar` |
| `height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "bottom_outer_radius": 8.0,
  "height": 8.0,
  "id": "funnel_tube",
  "kind": "FunnelTube",
  "segments": 16,
  "top_outer_radius": 6.0,
  "wall_thickness": 0.5
}
```

### `GearBlank`

GearBlank: a faceted cylinder with `tooth_count` rectangular notches cut around its outer circumference. Not a true involute gear (those need curve-aware extrusion), but a useful approximation for visualisation and mating-surface roughing.

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `root_radius` | `Scalar` |
| `tooth_count` | `usize` |
| `thickness` | `Scalar` |
| `segments_per_tooth` | `usize` |

**Example**

```json
{
  "id": "gear_blank",
  "kind": "GearBlank",
  "outer_radius": 8.0,
  "root_radius": 7.0,
  "segments_per_tooth": 4,
  "thickness": 2.0,
  "tooth_count": 12
}
```

### `GussetPlate`

GussetPlate: a right-triangle reinforcement plate — used at corners where two structural members meet. `leg_length` is each leg of the right triangle, `thickness` the plate thickness. Triangle has its right angle at the origin, legs along +x and +y.

**Parameters**

| field | type |
|---|---|
| `leg_length` | `Scalar` |
| `thickness` | `Scalar` |

**Example**

```json
{
  "id": "gusset_plate",
  "kind": "GussetPlate",
  "leg_length": 10.0,
  "thickness": 1.0
}
```

### `Hinge`

Hinge: two flat leaves joined by a cylindrical knuckle pin. Each leaf is `leaf_width` x `leaf_height` x `leaf_thickness`. The knuckle pin runs along +y between the two leaves with radius `pin_radius`. Pin and leaves are unioned into a single solid.

**Parameters**

| field | type |
|---|---|
| `leaf_width` | `Scalar` |
| `leaf_height` | `Scalar` |
| `leaf_thickness` | `Scalar` |
| `pin_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "hinge",
  "kind": "Hinge",
  "leaf_height": 10.0,
  "leaf_thickness": 1.0,
  "leaf_width": 10.0,
  "pin_radius": 2.0,
  "segments": 16
}
```

### `HoleyPlate`

HoleyPlate: a thick plate with a single circular hole in the center. Like PerforatedPlate but with one parametric hole.

**Parameters**

| field | type |
|---|---|
| `plate_width` | `Scalar` |
| `plate_height` | `Scalar` |
| `plate_thickness` | `Scalar` |
| `hole_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "hole_radius": 1.5,
  "id": "holey_plate",
  "kind": "HoleyPlate",
  "plate_height": 10.0,
  "plate_thickness": 1.0,
  "plate_width": 10.0,
  "segments": 16
}
```

### `IBeam`

I-beam (H-section) cross-section extruded along +z. Symmetric top and bottom flanges of `flange_width` × `flange_thickness`, centered web of `web_thickness` × (`total_height` − 2 × `flange_thickness`). `depth` is the extrusion length.

**Parameters**

| field | type |
|---|---|
| `flange_width` | `Scalar` |
| `flange_thickness` | `Scalar` |
| `web_thickness` | `Scalar` |
| `total_height` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "depth": 10.0,
  "flange_thickness": 1.0,
  "flange_width": 10.0,
  "id": "i_beam",
  "kind": "IBeam",
  "total_height": 10.0,
  "web_thickness": 1.0
}
```

### `LBracket`

L-shape (right-angle bracket) cross-section extruded along +z. `width` is the leg along +x, `height` is the leg along +y, both measured from the inner corner. `thickness` is the leg thickness. `depth` is the extrusion length along +z. Inner corner is at (thickness, thickness). Result fits in an axis- aligned bounding box of (width, height, depth).

**Parameters**

| field | type |
|---|---|
| `width` | `Scalar` |
| `height` | `Scalar` |
| `thickness` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "depth": 10.0,
  "height": 10.0,
  "id": "l_bracket",
  "kind": "LBracket",
  "thickness": 1.0,
  "width": 10.0
}
```

### `Lattice`

Lattice: a 2D grid of crossing rectangular bars. Like a gridiron. `nx` × `ny` cells, each `cell_size` square; bars are `bar_thickness` wide.

**Parameters**

| field | type |
|---|---|
| `nx` | `usize` |
| `ny` | `usize` |
| `cell_size` | `Scalar` |
| `bar_thickness` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "bar_thickness": 0.6,
  "cell_size": 4.0,
  "depth": 2.0,
  "id": "lattice",
  "kind": "Lattice",
  "nx": 3,
  "ny": 3
}
```

### `Lever`

Lever: a long rectangular bar with a pivot hole at one end. `length` is the full bar length, `width` and `thickness` the bar cross-section, `pivot_radius` the hole radius near the end.

**Parameters**

| field | type |
|---|---|
| `length` | `Scalar` |
| `width` | `Scalar` |
| `thickness` | `Scalar` |
| `pivot_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "lever",
  "kind": "Lever",
  "length": 10.0,
  "pivot_radius": 1.5,
  "segments": 16,
  "thickness": 1.0,
  "width": 10.0
}
```

### `MountingFlange`

MountingFlange: thin disk with a circular pattern of bolt holes. `disk_radius` is the outer flange radius, `disk_thickness` the flange thickness (along +z), `bolt_circle_radius` the radius of the bolt-hole circle, `bolt_count` the number of bolts, and `bolt_radius` the bolt-hole radius.

**Parameters**

| field | type |
|---|---|
| `disk_radius` | `Scalar` |
| `disk_thickness` | `Scalar` |
| `bolt_circle_radius` | `Scalar` |
| `bolt_count` | `usize` |
| `bolt_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "bolt_circle_radius": 6.0,
  "bolt_count": 4,
  "bolt_radius": 0.5,
  "disk_radius": 8.0,
  "disk_thickness": 1.0,
  "id": "mounting_flange",
  "kind": "MountingFlange",
  "segments": 16
}
```

### `OvalPlate`

OvalPlate: a flat plate with an elliptical outline. `a` is the semi-major axis along +x, `b` semi-minor along +y, extruded along +z by `thickness`.

**Parameters**

| field | type |
|---|---|
| `a` | `Scalar` |
| `b` | `Scalar` |
| `thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "a": 1.0,
  "b": 1.0,
  "id": "oval_plate",
  "kind": "OvalPlate",
  "segments": 16,
  "thickness": 1.0
}
```

### `ParapetWall`

ParapetWall: a wall with a series of square crenellations (battlements) along its top. `length` of wall, `wall_height` before merlons, `merlon_count`, each merlon `merlon_height` tall, `wall_thickness`.

**Parameters**

| field | type |
|---|---|
| `length` | `Scalar` |
| `wall_height` | `Scalar` |
| `wall_thickness` | `Scalar` |
| `merlon_count` | `usize` |
| `merlon_height` | `Scalar` |

**Example**

```json
{
  "id": "parapet_wall",
  "kind": "ParapetWall",
  "length": 10.0,
  "merlon_count": 4,
  "merlon_height": 1.0,
  "wall_height": 1.0,
  "wall_thickness": 1.0
}
```

### `Pediment`

Pediment: an architectural ornament — rectangular base with a triangular gable top. Extruded along +z by `depth`. Used in classical architecture above doors and windows.

**Parameters**

| field | type |
|---|---|
| `base_width` | `Scalar` |
| `base_height` | `Scalar` |
| `gable_height` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "base_height": 10.0,
  "base_width": 10.0,
  "depth": 10.0,
  "gable_height": 1.0,
  "id": "pediment",
  "kind": "Pediment"
}
```

### `PerforatedPlate`

PerforatedPlate: rectangular plate with a regular grid of circular through-holes. `nx` × `ny` grid; first hole at `(margin, margin)`; spacing `dx` along x, `dy` along y.

**Parameters**

| field | type |
|---|---|
| `plate_width` | `Scalar` |
| `plate_height` | `Scalar` |
| `plate_thickness` | `Scalar` |
| `margin` | `Scalar` |
| `hole_radius` | `Scalar` |
| `nx` | `usize` |
| `ny` | `usize` |
| `dx` | `Scalar` |
| `dy` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "dx": 5.0,
  "dy": 5.0,
  "hole_radius": 0.6,
  "id": "perforated_plate",
  "kind": "PerforatedPlate",
  "margin": 2.0,
  "nx": 3,
  "ny": 3,
  "plate_height": 20.0,
  "plate_thickness": 1.0,
  "plate_width": 20.0,
  "segments": 12
}
```

### `PinShaft`

PinShaft: a long, narrow cylindrical pin — like AxleShaft but with one diameter throughout. Useful for hinge pins, dowels.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "pin_shaft",
  "kind": "PinShaft",
  "length": 10.0,
  "radius": 5.0,
  "segments": 16
}
```

### `Pipe`

Pipe: cylindrical tube whose axis is the given axis-aligned line (currently restricted to "x" / "y" / "z" — like Tube but the orientation is named, not implicit z). Outer / inner radii along the axis, length along that axis from the base point.

**Parameters**

| field | type |
|---|---|
| `base` | `[Scalar; 3]` |
| `axis` | `String` |
| `outer_radius` | `Scalar` |
| `inner_radius` | `Scalar` |
| `length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "axis": "z",
  "base": [
    0.0,
    0.0,
    0.0
  ],
  "id": "pipe",
  "inner_radius": 3.0,
  "kind": "Pipe",
  "length": 10.0,
  "outer_radius": 5.0,
  "segments": 16
}
```

### `RatchetTooth`

RatchetTooth: an asymmetric sawtooth profile — N teeth, each with a vertical face and a sloped face. Useful for ratchet wheels and one-way clutches.

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `root_radius` | `Scalar` |
| `tooth_count` | `usize` |
| `thickness` | `Scalar` |

**Example**

```json
{
  "id": "ratchet_tooth",
  "kind": "RatchetTooth",
  "outer_radius": 8.0,
  "root_radius": 6.5,
  "thickness": 1.0,
  "tooth_count": 4
}
```

### `RectBoss`

Rectangular boss raised from xy plane: a Box of `extents` positioned at `corner`. Provided as a feature for ergonomics — equivalent to `BoxAt(extents, corner)`.

**Parameters**

| field | type |
|---|---|
| `corner` | `[Scalar; 3]` |
| `extents` | `[Scalar; 3]` |

**Example**

```json
{
  "corner": [
    3.0,
    3.0,
    3.0
  ],
  "extents": [
    10.0,
    10.0,
    10.0
  ],
  "id": "rect_boss",
  "kind": "RectBoss"
}
```

### `ReducerCone`

ReducerCone: a hollow frustum (a tapered tube). Concentric outer and inner cones — outer goes from `outer_bottom` radius to `outer_top` over `height`; inner same shape but smaller. Used for pipe reducers.

**Parameters**

| field | type |
|---|---|
| `outer_bottom` | `Scalar` |
| `outer_top` | `Scalar` |
| `inner_bottom` | `Scalar` |
| `inner_top` | `Scalar` |
| `height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "height": 8.0,
  "id": "reducer_cone",
  "inner_bottom": 5.0,
  "inner_top": 3.0,
  "kind": "ReducerCone",
  "outer_bottom": 6.0,
  "outer_top": 4.0,
  "segments": 16
}
```

### `RibbedPlate`

RibbedPlate: a flat plate with parallel rectangular ribs along its top surface. `n_ribs` evenly spaced; each rib is `rib_width` wide and `rib_height` tall.

**Parameters**

| field | type |
|---|---|
| `plate_length` | `Scalar` |
| `plate_width` | `Scalar` |
| `plate_thickness` | `Scalar` |
| `n_ribs` | `usize` |
| `rib_width` | `Scalar` |
| `rib_height` | `Scalar` |

**Example**

```json
{
  "id": "ribbed_plate",
  "kind": "RibbedPlate",
  "n_ribs": 4,
  "plate_length": 30.0,
  "plate_thickness": 1.0,
  "plate_width": 10.0,
  "rib_height": 1.5,
  "rib_width": 2.0
}
```

### `RoundBoss`

Cylindrical boss raised from xy plane: a Cylinder of `radius` and `height`, positioned at `base` (center of the bottom cap). Provided as a feature for ergonomics — equivalent to `Translate(Cylinder(...), base)`.

**Parameters**

| field | type |
|---|---|
| `base` | `[Scalar; 3]` |
| `radius` | `Scalar` |
| `height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "base": [
    0.0,
    0.0,
    0.0
  ],
  "height": 10.0,
  "id": "round_boss",
  "kind": "RoundBoss",
  "radius": 5.0,
  "segments": 16
}
```

### `RoundedRect`

Rectangular plate with rounded corners (4 quarter-circle fillets, each with the same `corner_radius`). Useful for mounting plates where sharp corners would cause stress concentrations.

**Parameters**

| field | type |
|---|---|
| `width` | `Scalar` |
| `height` | `Scalar` |
| `thickness` | `Scalar` |
| `corner_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "corner_radius": 2.0,
  "height": 10.0,
  "id": "rounded_rect",
  "kind": "RoundedRect",
  "segments": 16,
  "thickness": 1.0,
  "width": 10.0
}
```

### `SBend`

SBend: an S-shaped pipe — two opposite 90° arcs joined. Useful for offsetting pipes around obstacles.

**Parameters**

| field | type |
|---|---|
| `bend_radius` | `Scalar` |
| `pipe_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "bend_radius": 6.0,
  "id": "s_bend",
  "kind": "SBend",
  "pipe_radius": 1.0,
  "segments": 16
}
```

### `ScrewBoss`

ScrewBoss: a cylindrical boss with a central pre-drilled hole for self-tapping screws. Outer cylinder + concentric narrower hole that doesn't go through (blind hole).

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `outer_height` | `Scalar` |
| `hole_radius` | `Scalar` |
| `hole_depth` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "hole_depth": 1.0,
  "hole_radius": 1.5,
  "id": "screw_boss",
  "kind": "ScrewBoss",
  "outer_height": 10.0,
  "outer_radius": 8.0,
  "segments": 16
}
```

### `ScrollPlate`

ScrollPlate: a flat S-curve scroll plate — two oppositely-curving arc segments joined. Decorative ornament.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `rod_radius` | `Scalar` |
| `thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "scroll_plate",
  "kind": "ScrollPlate",
  "radius": 5.0,
  "rod_radius": 2.0,
  "segments": 16,
  "thickness": 1.0
}
```

### `SheetBend`

SheetBend: a thin sheet-metal shape that bends 90° at a given distance from the start. First section runs `length_a` along +x at thickness `thickness`, then bends up 90° and continues `length_b` along +z. `width` is the depth along +y.

**Parameters**

| field | type |
|---|---|
| `length_a` | `Scalar` |
| `length_b` | `Scalar` |
| `width` | `Scalar` |
| `thickness` | `Scalar` |

**Example**

```json
{
  "id": "sheet_bend",
  "kind": "SheetBend",
  "length_a": 10.0,
  "length_b": 10.0,
  "thickness": 1.0,
  "width": 10.0
}
```

### `ShelfBracket`

ShelfBracket: an L-shaped bracket with a diagonal brace from the bottom of the vertical leg to the end of the horizontal leg. Adds rigidity to a simple LBracket.

**Parameters**

| field | type |
|---|---|
| `horizontal` | `Scalar` |
| `vertical` | `Scalar` |
| `thickness` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "depth": 6.0,
  "horizontal": 8.0,
  "id": "shelf_bracket",
  "kind": "ShelfBracket",
  "thickness": 1.0,
  "vertical": 10.0
}
```

### `SpiralPlate`

SpiralPlate: an Archimedean-spiral pattern of N revolutions approximated as a chain of straight segments swept as a thin rod. Decorative.

**Parameters**

| field | type |
|---|---|
| `max_radius` | `Scalar` |
| `revolutions` | `Scalar` |
| `rod_radius` | `Scalar` |
| `z` | `Scalar` |
| `segments_per_revolution` | `usize` |

**Example**

```json
{
  "id": "spiral_plate",
  "kind": "SpiralPlate",
  "max_radius": 8.0,
  "revolutions": 2.0,
  "rod_radius": 0.4,
  "segments_per_revolution": 12,
  "z": 1.0
}
```

### `SquareTube`

SquareTube: rectangular hollow extruded tube. Like UChannel but closed on top — outer rect minus inner rect cavity, all four sides closed.

**Parameters**

| field | type |
|---|---|
| `outer_width` | `Scalar` |
| `outer_height` | `Scalar` |
| `wall_thickness` | `Scalar` |
| `length` | `Scalar` |

**Example**

```json
{
  "id": "square_tube",
  "kind": "SquareTube",
  "length": 10.0,
  "outer_height": 10.0,
  "outer_width": 10.0,
  "wall_thickness": 1.0
}
```

### `Stair`

Stair: a parametric staircase — `step_count` steps, each `tread` deep (along +y) and `riser` tall (along +z). Total run = count*tread, total rise = count*riser. Width = `step_width` along +x.

**Parameters**

| field | type |
|---|---|
| `step_count` | `usize` |
| `tread` | `Scalar` |
| `riser` | `Scalar` |
| `step_width` | `Scalar` |

**Example**

```json
{
  "id": "stair",
  "kind": "Stair",
  "riser": 3.0,
  "step_count": 4,
  "step_width": 10.0,
  "tread": 5.0
}
```

### `Stake`

Stake: a long shaft tapering to a sharp tip. Cylinder body (`body_radius`, `body_length`) + cone tip (`tip_length`). Used for survey stakes, ground anchors.

**Parameters**

| field | type |
|---|---|
| `body_radius` | `Scalar` |
| `body_length` | `Scalar` |
| `tip_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "body_length": 10.0,
  "body_radius": 5.0,
  "id": "stake",
  "kind": "Stake",
  "segments": 16,
  "tip_length": 3.0
}
```

### `SteppedShaft`

SteppedShaft: cylinder with monotonically decreasing diameter steps. `radii` is a list of radii (descending recommended) and `step_lengths` is the length of each step. Both must be the same length.

**Parameters**

| field | type |
|---|---|
| `radii` | `Vec<Scalar>` |
| `step_lengths` | `Vec<Scalar>` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "stepped_shaft",
  "kind": "SteppedShaft",
  "radii": [
    5.0,
    4.0,
    3.0
  ],
  "segments": 16,
  "step_lengths": [
    10.0,
    8.0,
    6.0
  ]
}
```

### `TBeam`

T-beam cross-section extruded along +z. The flange is a horizontal bar of `flange_width` × `flange_thickness` sitting on top; the web is a vertical bar of `web_thickness` × (`total_height` − `flange_thickness`) below it, centered along the flange. `depth` is the extrusion length.

**Parameters**

| field | type |
|---|---|
| `flange_width` | `Scalar` |
| `flange_thickness` | `Scalar` |
| `web_thickness` | `Scalar` |
| `total_height` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "depth": 10.0,
  "flange_thickness": 1.0,
  "flange_width": 10.0,
  "id": "t_beam",
  "kind": "TBeam",
  "total_height": 10.0,
  "web_thickness": 1.0
}
```

### `TaperedTube`

TaperedTube: hollow frustum — same as ReducerCone but with a constant wall thickness instead of named inner/outer radii at each end. Easier to specify when you just know "tapered tube with X-thick walls".

**Parameters**

| field | type |
|---|---|
| `bottom_outer_radius` | `Scalar` |
| `top_outer_radius` | `Scalar` |
| `wall_thickness` | `Scalar` |
| `height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "bottom_outer_radius": 6.0,
  "height": 10.0,
  "id": "tapered_tube",
  "kind": "TaperedTube",
  "segments": 16,
  "top_outer_radius": 4.0,
  "wall_thickness": 0.7
}
```

### `Tee`

Tee: a 3-way pipe junction. Three cylinders meeting at the origin, all the same radius. Two run along the same axis (e.g. +x and -x), the third runs perpendicular (+y).

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `leg_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "tee",
  "kind": "Tee",
  "leg_length": 10.0,
  "radius": 5.0,
  "segments": 16
}
```

### `Trapezoid`

Trapezoid: an isosceles trapezoidal prism extruded along +z. Useful for keyway profiles and tapered joints.

**Parameters**

| field | type |
|---|---|
| `bottom_width` | `Scalar` |
| `top_width` | `Scalar` |
| `height` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "bottom_width": 10.0,
  "depth": 10.0,
  "height": 10.0,
  "id": "trapezoid",
  "kind": "Trapezoid",
  "top_width": 10.0
}
```

### `TriPrism`

TriPrism: a triangular prism. `a`, `b`, `c` are the side lengths of the triangular profile (must satisfy triangle inequality); triangle is positioned with side a along +x. `length` is the extrusion along +z.

**Parameters**

| field | type |
|---|---|
| `a` | `Scalar` |
| `b` | `Scalar` |
| `c` | `Scalar` |
| `length` | `Scalar` |

**Example**

```json
{
  "a": 1.0,
  "b": 1.0,
  "c": 1.0,
  "id": "tri_prism",
  "kind": "TriPrism",
  "length": 10.0
}
```

### `TriangularPlate`

TriangularPlate: an equilateral or arbitrary triangle plate extruded by `thickness`. Specified by 3 (x, y) corner points.

**Parameters**

| field | type |
|---|---|
| `a` | `[Scalar; 2]` |
| `b` | `[Scalar; 2]` |
| `c` | `[Scalar; 2]` |
| `thickness` | `Scalar` |

**Example**

```json
{
  "a": [
    0.0,
    0.0
  ],
  "b": [
    10.0,
    0.0
  ],
  "c": [
    5.0,
    8.0
  ],
  "id": "triangular_plate",
  "kind": "TriangularPlate",
  "thickness": 1.0
}
```

### `TrussMember`

TrussMember: a thin cylindrical rod with rectangular end plates — used to model truss bars or struts. The rod runs along +z from end plate to end plate.

**Parameters**

| field | type |
|---|---|
| `rod_radius` | `Scalar` |
| `rod_length` | `Scalar` |
| `plate_width` | `Scalar` |
| `plate_thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "truss_member",
  "kind": "TrussMember",
  "plate_thickness": 1.0,
  "plate_width": 10.0,
  "rod_length": 10.0,
  "rod_radius": 2.0,
  "segments": 16
}
```

### `Tube`

Hollow circular tube: outer cylinder minus a centered inner cylinder. Both share the same axis (origin, +z direction) and height.

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `inner_radius` | `Scalar` |
| `height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "height": 10.0,
  "id": "tube",
  "inner_radius": 3.0,
  "kind": "Tube",
  "outer_radius": 5.0,
  "segments": 16
}
```

### `TubeAt`

Tube (hollow cylinder) at an axis-aligned position with chosen edge axis. Same orientation rules as `CylinderAt`. Inner cylinder is automatically extended past both caps so the bore is a clean through-hole regardless of orientation.

**Parameters**

| field | type |
|---|---|
| `base` | `[Scalar; 3]` |
| `axis` | `String` |
| `outer_radius` | `Scalar` |
| `inner_radius` | `Scalar` |
| `height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "axis": "z",
  "base": [
    0.0,
    0.0,
    0.0
  ],
  "height": 10.0,
  "id": "tube_at",
  "inner_radius": 3.0,
  "kind": "TubeAt",
  "outer_radius": 5.0,
  "segments": 16
}
```

### `UBendPipe`

UBendPipe: a 180° toroidal pipe section — like a U-bend in plumbing. `bend_radius` is the centerline radius of the bend, `pipe_radius` the pipe outer radius. Lies in the xz plane, from (0, 0, 0) curving up and around to (2*bend_radius, 0, 0).

**Parameters**

| field | type |
|---|---|
| `bend_radius` | `Scalar` |
| `pipe_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "bend_radius": 6.0,
  "id": "u_bend_pipe",
  "kind": "UBendPipe",
  "pipe_radius": 1.0,
  "segments": 16
}
```

### `UChannel`

U-channel cross-section extruded along +z. `width` is the outside x-extent, `height` is the y-extent of the legs (web at y=0). `thickness` is the wall thickness for both legs and the web. `depth` is the extrusion length. Result occupies x ∈ [0, width], y ∈ [0, height], z ∈ [0, depth].

**Parameters**

| field | type |
|---|---|
| `width` | `Scalar` |
| `height` | `Scalar` |
| `thickness` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "depth": 10.0,
  "height": 10.0,
  "id": "u_channel",
  "kind": "UChannel",
  "thickness": 1.0,
  "width": 10.0
}
```

### `Vault`

Vault: a rectangular base with a half-cylinder vault on top — the classic vaulted ceiling profile. Half-cylinder spans the full base_width; vault axis runs along +y.

**Parameters**

| field | type |
|---|---|
| `base_width` | `Scalar` |
| `base_length` | `Scalar` |
| `base_height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "base_height": 10.0,
  "base_length": 1.0,
  "base_width": 10.0,
  "id": "vault",
  "kind": "Vault",
  "segments": 16
}
```

### `WindowFrame`

WindowFrame: a rectangular frame — outer rectangle minus inner rectangular cutout, extruded along +z. Like SquaredRing but with explicit "window frame" intent and asymmetric inner offset.

**Parameters**

| field | type |
|---|---|
| `outer_width` | `Scalar` |
| `outer_height` | `Scalar` |
| `frame_thickness` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "depth": 10.0,
  "frame_thickness": 1.0,
  "id": "window_frame",
  "kind": "WindowFrame",
  "outer_height": 10.0,
  "outer_width": 10.0
}
```

### `WindowLouver`

WindowLouver: an outer rectangular frame (window-frame style) with N horizontal angled slats inside. The frame outer is `outer_width` x `outer_height` with frame walls of `frame_thickness`, extruded by `depth`. `slats` rectangular slats of dimensions (inner_width, slat_thickness, depth) are stacked inside the inner cavity, evenly spaced. (Slats currently axis-aligned; the angle parameter is recorded for future tilt support.)

**Parameters**

| field | type |
|---|---|
| `outer_width` | `Scalar` |
| `outer_height` | `Scalar` |
| `frame_thickness` | `Scalar` |
| `depth` | `Scalar` |
| `slats` | `usize` |
| `slat_thickness` | `Scalar` |

**Example**

```json
{
  "depth": 10.0,
  "frame_thickness": 1.0,
  "id": "window_louver",
  "kind": "WindowLouver",
  "outer_height": 10.0,
  "outer_width": 10.0,
  "slat_thickness": 1.0,
  "slats": 4
}
```

### `WireMesh`

WireMesh: a thin grid of crossing rods (cylinders) instead of rectangular bars. Like Lattice but with cylindrical bars.

**Parameters**

| field | type |
|---|---|
| `nx` | `usize` |
| `ny` | `usize` |
| `cell_size` | `Scalar` |
| `wire_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "cell_size": 4.0,
  "id": "wire_mesh",
  "kind": "WireMesh",
  "nx": 3,
  "ny": 3,
  "segments": 8,
  "wire_radius": 0.3
}
```

### `Yoke`

Yoke: a U-shaped fork — two parallel arms with a connecting crossbar, plus an optional pivot hole through both arms. Used for clevis and shackle joints.

**Parameters**

| field | type |
|---|---|
| `arm_length` | `Scalar` |
| `arm_thickness` | `Scalar` |
| `arm_height` | `Scalar` |
| `gap_width` | `Scalar` |
| `pivot_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "arm_height": 10.0,
  "arm_length": 10.0,
  "arm_thickness": 1.0,
  "gap_width": 1.0,
  "id": "yoke",
  "kind": "Yoke",
  "pivot_radius": 1.5,
  "segments": 16
}
```

### `ZBeam`

Z-beam (Z-bar) cross-section extruded along +z. Three rectangular runs forming a "Z": bottom horizontal flange of length `flange`, vertical web of height `web`, top horizontal flange of length `flange` (offset to the right by web_thickness). Each run has thickness `thickness`.

**Parameters**

| field | type |
|---|---|
| `flange` | `Scalar` |
| `web` | `Scalar` |
| `thickness` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "depth": 20.0,
  "flange": 8.0,
  "id": "z_beam",
  "kind": "ZBeam",
  "thickness": 1.0,
  "web": 10.0
}
```

## Vessels & containers

### `Bottle`

Bottle: a cylindrical body with a narrow cylindrical neck on top. `body_radius`/`body_height` define the lower part; the neck is a smaller cylinder of `neck_radius`/`neck_height` above.

**Parameters**

| field | type |
|---|---|
| `body_radius` | `Scalar` |
| `body_height` | `Scalar` |
| `neck_radius` | `Scalar` |
| `neck_height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "body_height": 10.0,
  "body_radius": 4.0,
  "id": "bottle",
  "kind": "Bottle",
  "neck_height": 3.0,
  "neck_radius": 1.5,
  "segments": 16
}
```

### `Bowl`

Bowl: hollow hemisphere shell. Outer hemisphere of `outer_radius` minus inner hemisphere of `inner_radius`. Wall thickness = outer - inner. Open top, like a dome inverted.

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `inner_radius` | `Scalar` |
| `stacks` | `usize` |
| `slices` | `usize` |

**Example**

```json
{
  "id": "bowl",
  "inner_radius": 4.0,
  "kind": "Bowl",
  "outer_radius": 5.0,
  "slices": 8,
  "stacks": 4
}
```

### `CableSaddle`

CableSaddle: a U-shaped clamp — a rectangular base with a semicylindrical groove on top to receive a cable.

**Parameters**

| field | type |
|---|---|
| `base_length` | `Scalar` |
| `base_width` | `Scalar` |
| `base_height` | `Scalar` |
| `cable_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "base_height": 10.0,
  "base_length": 1.0,
  "base_width": 10.0,
  "cable_radius": 1.0,
  "id": "cable_saddle",
  "kind": "CableSaddle",
  "segments": 16
}
```

### `Cup`

Cup: an open-top cylindrical container — outer cylinder minus inner cylindrical cavity. `wall_thickness` defines wall + floor.

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `height` | `Scalar` |
| `wall_thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "height": 8.0,
  "id": "cup",
  "kind": "Cup",
  "outer_radius": 5.0,
  "segments": 16,
  "wall_thickness": 0.7
}
```

### `FishingFloat`

FishingFloat: a sphere with a small cylindrical pin sticking out the top (and a smaller one out the bottom). Stylised fishing-float silhouette.

**Parameters**

| field | type |
|---|---|
| `body_radius` | `Scalar` |
| `pin_radius` | `Scalar` |
| `pin_length` | `Scalar` |
| `stacks` | `usize` |
| `slices` | `usize` |

**Example**

```json
{
  "body_radius": 5.0,
  "id": "fishing_float",
  "kind": "FishingFloat",
  "pin_length": 1.0,
  "pin_radius": 2.0,
  "slices": 16,
  "stacks": 16
}
```

### `Funnel`

Funnel: a frustum (wide at top) connected to a smaller cylinder (the spout) at the bottom. The frustum sits at z ∈ [0, neck_z] with `top_radius` at z=neck_z and `neck_radius` at z=0. The spout cylinder of `neck_radius` sits at z ∈ [-spout_length, 0].

**Parameters**

| field | type |
|---|---|
| `top_radius` | `Scalar` |
| `neck_radius` | `Scalar` |
| `neck_z` | `Scalar` |
| `spout_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "funnel",
  "kind": "Funnel",
  "neck_radius": 2.0,
  "neck_z": 6.0,
  "segments": 16,
  "spout_length": 4.0,
  "top_radius": 8.0
}
```

### `HollowBox`

Box with a uniform inset wall thickness on all six faces. Equivalent to BoxAt(extents, [0,0,0]) minus BoxAt(extents - 2*wall, [wall,wall,wall]).

**Parameters**

| field | type |
|---|---|
| `extents` | `[Scalar; 3]` |
| `wall_thickness` | `Scalar` |

**Example**

```json
{
  "extents": [
    10.0,
    10.0,
    10.0
  ],
  "id": "hollow_box",
  "kind": "HollowBox",
  "wall_thickness": 1.0
}
```

### `HollowBrick`

HollowBrick: a brick with a rectangular interior cavity (a classic cinder block). Open top.

**Parameters**

| field | type |
|---|---|
| `length` | `Scalar` |
| `width` | `Scalar` |
| `height` | `Scalar` |
| `wall_thickness` | `Scalar` |

**Example**

```json
{
  "height": 4.0,
  "id": "hollow_brick",
  "kind": "HollowBrick",
  "length": 10.0,
  "wall_thickness": 0.7,
  "width": 6.0
}
```

### `HollowCone`

HollowCone: a cone with an axial bore. Outer cone (faceted) minus a central cylindrical hole.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `height` | `Scalar` |
| `bore_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "bore_radius": 3.0,
  "height": 10.0,
  "id": "hollow_cone",
  "kind": "HollowCone",
  "radius": 5.0,
  "segments": 16
}
```

### `HollowCylinder`

Closed-end hollow cylinder: tube with `end_thickness`-thick caps. `outer_radius`, `inner_radius`, `height`, `end_thickness`, `segments`. Inner cavity has length `height - 2 * end_thickness`.

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `inner_radius` | `Scalar` |
| `height` | `Scalar` |
| `end_thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "end_thickness": 0.5,
  "height": 10.0,
  "id": "hollow_cylinder",
  "inner_radius": 3.0,
  "kind": "HollowCylinder",
  "outer_radius": 5.0,
  "segments": 16
}
```

### `HollowSphere`

Hollow spherical shell: outer faceted sphere of `outer_radius` minus an inner faceted sphere of `inner_radius`. Concentric on the origin. `stacks` and `slices` apply to both spheres for matching topology.

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `inner_radius` | `Scalar` |
| `stacks` | `usize` |
| `slices` | `usize` |

**Example**

```json
{
  "id": "hollow_sphere",
  "inner_radius": 3.0,
  "kind": "HollowSphere",
  "outer_radius": 5.0,
  "slices": 8,
  "stacks": 4
}
```

### `Hopper`

Hopper: a cone funnel atop a cylindrical neck. Used for gravity-feed bins, pour spouts. `top_radius` is the wide top of the funnel, `neck_radius` the narrow bottom that joins the vertical neck. `funnel_height` is the funnel cone height, `neck_height` the neck cylinder height.

**Parameters**

| field | type |
|---|---|
| `top_radius` | `Scalar` |
| `neck_radius` | `Scalar` |
| `funnel_height` | `Scalar` |
| `neck_height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "funnel_height": 8.0,
  "id": "hopper",
  "kind": "Hopper",
  "neck_height": 4.0,
  "neck_radius": 3.0,
  "segments": 16,
  "top_radius": 8.0
}
```

### `Lampshade`

Lampshade: an open hollow frustum — same as FunnelTube but with explicit "lampshade" intent and a guaranteed open top (no rim).

**Parameters**

| field | type |
|---|---|
| `top_radius` | `Scalar` |
| `bottom_radius` | `Scalar` |
| `height` | `Scalar` |
| `wall_thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "bottom_radius": 6.0,
  "height": 10.0,
  "id": "lampshade",
  "kind": "Lampshade",
  "segments": 16,
  "top_radius": 3.0,
  "wall_thickness": 1.0
}
```

### `PlanterBox`

PlanterBox: an open-top rectangular planter — like Trough but with `outer_length` instead of square cross-section.

**Parameters**

| field | type |
|---|---|
| `outer_width` | `Scalar` |
| `outer_length` | `Scalar` |
| `outer_height` | `Scalar` |
| `wall_thickness` | `Scalar` |

**Example**

```json
{
  "id": "planter_box",
  "kind": "PlanterBox",
  "outer_height": 8.0,
  "outer_length": 10.0,
  "outer_width": 15.0,
  "wall_thickness": 0.8
}
```

### `Spool`

Spool: a cable reel — a cylinder body with two wider disk flanges on each end. `body_radius` < `flange_radius`, `body_length` is the central cylinder length, `flange_thickness` the thickness of each end disk.

**Parameters**

| field | type |
|---|---|
| `body_radius` | `Scalar` |
| `body_length` | `Scalar` |
| `flange_radius` | `Scalar` |
| `flange_thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "body_length": 10.0,
  "body_radius": 5.0,
  "flange_radius": 7.0,
  "flange_thickness": 1.0,
  "id": "spool",
  "kind": "Spool",
  "segments": 16
}
```

### `Trough`

Trough: long U-shaped channel — same cross-section as UChannel but with closed ends (front and back walls). Useful for tray / drainage channel modelling.

**Parameters**

| field | type |
|---|---|
| `outer_width` | `Scalar` |
| `outer_height` | `Scalar` |
| `wall_thickness` | `Scalar` |
| `length` | `Scalar` |

**Example**

```json
{
  "id": "trough",
  "kind": "Trough",
  "length": 20.0,
  "outer_height": 6.0,
  "outer_width": 10.0,
  "wall_thickness": 0.8
}
```

## Furniture

### `Bench`

Bench: a long rectangular seat slab on two box-shaped supports. The seat spans x ∈ [0, length], y ∈ [0, depth], z ∈ [leg_height, leg_height + seat_thickness]. Two supports of `support_thickness` x `depth` x `leg_height` sit at each end, inset from the seat ends by `support_inset`.

**Parameters**

| field | type |
|---|---|
| `length` | `Scalar` |
| `depth` | `Scalar` |
| `seat_thickness` | `Scalar` |
| `leg_height` | `Scalar` |
| `support_thickness` | `Scalar` |
| `support_inset` | `Scalar` |

**Example**

```json
{
  "depth": 10.0,
  "id": "bench",
  "kind": "Bench",
  "leg_height": 10.0,
  "length": 10.0,
  "seat_thickness": 1.0,
  "support_inset": 1.0,
  "support_thickness": 1.0
}
```

### `Bookshelf`

Bookshelf: a multi-shelf rectangular shelving unit. `shelves` horizontal panels of size (width × depth × shelf_thickness) stacked at intervals of `clear_height`. Two side panels of height = shelves * (clear_height + shelf_thickness).

**Parameters**

| field | type |
|---|---|
| `width` | `Scalar` |
| `depth` | `Scalar` |
| `shelves` | `usize` |
| `shelf_thickness` | `Scalar` |
| `clear_height` | `Scalar` |
| `side_thickness` | `Scalar` |

**Example**

```json
{
  "clear_height": 8.0,
  "depth": 10.0,
  "id": "bookshelf",
  "kind": "Bookshelf",
  "shelf_thickness": 1.0,
  "shelves": 3,
  "side_thickness": 1.0,
  "width": 30.0
}
```

### `Chair`

Chair: a square seat box on four legs, with a vertical back rest extending up from the rear edge. Seat sits at z ∈ [leg_height, leg_height + seat_thickness], spanning x ∈ [0, seat_size] and y ∈ [0, seat_size]. Four box legs of (leg_thickness)² x leg_height, inset from corners by `leg_inset`. Back rest is a vertical box at y ∈ [0, back_thickness], x ∈ [0, seat_size], z ∈ [leg_height + seat_thickness, leg_height + seat_thickness + back_height].

**Parameters**

| field | type |
|---|---|
| `seat_size` | `Scalar` |
| `seat_thickness` | `Scalar` |
| `leg_height` | `Scalar` |
| `leg_thickness` | `Scalar` |
| `leg_inset` | `Scalar` |
| `back_thickness` | `Scalar` |
| `back_height` | `Scalar` |

**Example**

```json
{
  "back_height": 12.0,
  "back_thickness": 1.0,
  "id": "chair",
  "kind": "Chair",
  "leg_height": 12.0,
  "leg_inset": 1.0,
  "leg_thickness": 1.0,
  "seat_size": 10.0,
  "seat_thickness": 1.0
}
```

### `ChairLeg`

ChairLeg: a rectangular leg — extruded square cross-section.

**Parameters**

| field | type |
|---|---|
| `width` | `Scalar` |
| `depth` | `Scalar` |
| `height` | `Scalar` |

**Example**

```json
{
  "depth": 10.0,
  "height": 10.0,
  "id": "chair_leg",
  "kind": "ChairLeg",
  "width": 10.0
}
```

### `Column`

Column: classical column with a wider base, narrower shaft, and wider capital. base/capital are short cylinders; shaft is the main cylinder. `base_radius` >= `shaft_radius` <= `capital_radius`.

**Parameters**

| field | type |
|---|---|
| `base_radius` | `Scalar` |
| `base_height` | `Scalar` |
| `shaft_radius` | `Scalar` |
| `shaft_height` | `Scalar` |
| `capital_radius` | `Scalar` |
| `capital_height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "base_height": 10.0,
  "base_radius": 8.0,
  "capital_height": 10.0,
  "capital_radius": 8.0,
  "id": "column",
  "kind": "Column",
  "segments": 16,
  "shaft_height": 10.0,
  "shaft_radius": 2.0
}
```

### `Obelisk`

Obelisk: 4-sided tapered prism with a square bottom and a smaller square top. Centered on the origin, axis along +z. Special case of TruncatedPyramid with 4 sides.

**Parameters**

| field | type |
|---|---|
| `bottom_side` | `Scalar` |
| `top_side` | `Scalar` |
| `height` | `Scalar` |

**Example**

```json
{
  "bottom_side": 8.0,
  "height": 20.0,
  "id": "obelisk",
  "kind": "Obelisk",
  "top_side": 3.0
}
```

### `Plinth`

Plinth: a stepped square base — a wider square slab at the bottom, a narrower square slab on top. Used as a column or pedestal base.

**Parameters**

| field | type |
|---|---|
| `bottom_side` | `Scalar` |
| `bottom_height` | `Scalar` |
| `top_side` | `Scalar` |
| `top_height` | `Scalar` |

**Example**

```json
{
  "bottom_height": 1.0,
  "bottom_side": 10.0,
  "id": "plinth",
  "kind": "Plinth",
  "top_height": 1.0,
  "top_side": 10.0
}
```

### `Stand`

Stand: a cylindrical platform — a thick disk of (radius, thickness). Useful for workpiece supports.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "stand",
  "kind": "Stand",
  "radius": 5.0,
  "segments": 16,
  "thickness": 1.0
}
```

### `TableLeg`

TableLeg: a tapered cylindrical leg — wider at the bottom, narrower at the top. Same params as Plug.

**Parameters**

| field | type |
|---|---|
| `bottom_radius` | `Scalar` |
| `top_radius` | `Scalar` |
| `height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "bottom_radius": 5.0,
  "height": 40.0,
  "id": "table_leg",
  "kind": "TableLeg",
  "segments": 16,
  "top_radius": 3.0
}
```

### `TableTop`

TableTop: a rectangular slab top supported by four cylindrical legs at the corners. The slab spans x ∈ [0, top_width], y ∈ [0, top_depth] and sits at z ∈ [leg_height, leg_height + top_thickness]. Each leg is a cylinder of `leg_radius` running from z=0 to z=leg_height, inset from each corner by `leg_inset`. Legs poke 1e-3 into the slab to seal the boolean union.

**Parameters**

| field | type |
|---|---|
| `top_width` | `Scalar` |
| `top_depth` | `Scalar` |
| `top_thickness` | `Scalar` |
| `leg_radius` | `Scalar` |
| `leg_height` | `Scalar` |
| `leg_inset` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "table_top",
  "kind": "TableTop",
  "leg_height": 20.0,
  "leg_inset": 2.0,
  "leg_radius": 1.0,
  "segments": 16,
  "top_depth": 20.0,
  "top_thickness": 1.5,
  "top_width": 40.0
}
```

## Tools & hardware

### `Antiprism`

Antiprism: an N-gon at z=0 and an N-gon at z=`height`, where the top N-gon is rotated by π/N relative to the bottom (so vertices alternate). Connecting lateral faces are triangles (which is what makes it an antiprism rather than a prism).

**Parameters**

| field | type |
|---|---|
| `n_sides` | `usize` |
| `radius` | `Scalar` |
| `height` | `Scalar` |

**Example**

```json
{
  "height": 10.0,
  "id": "antiprism",
  "kind": "Antiprism",
  "n_sides": 4,
  "radius": 5.0
}
```

### `Bipyramid`

Bipyramid: two N-sided pyramids joined base-to-base. `n_sides` for the equator polygon, `radius` for the equator circumradius, `top_height` and `bottom_height` for each pyramid's apex distance from the equator.

**Parameters**

| field | type |
|---|---|
| `n_sides` | `usize` |
| `radius` | `Scalar` |
| `top_height` | `Scalar` |
| `bottom_height` | `Scalar` |

**Example**

```json
{
  "bottom_height": 1.0,
  "id": "bipyramid",
  "kind": "Bipyramid",
  "n_sides": 4,
  "radius": 5.0,
  "top_height": 1.0
}
```

### `Brick`

Brick: a standard masonry brick with an optional shallow cylindrical "frog" depression on top (indent for mortar).

**Parameters**

| field | type |
|---|---|
| `length` | `Scalar` |
| `width` | `Scalar` |
| `height` | `Scalar` |
| `frog_radius` | `Scalar` |
| `frog_depth` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "frog_depth": 1.0,
  "frog_radius": 1.0,
  "height": 10.0,
  "id": "brick",
  "kind": "Brick",
  "length": 10.0,
  "segments": 16,
  "width": 10.0
}
```

### `Bushing`

Bushing: a tube with a wider flange at one end (top-hat profile). `inner_radius` is the bore, `outer_radius` the sleeve outside, `flange_radius` the wider flange disk, `flange_thickness` the flange's thickness, `body_length` the sleeve length below the flange.

**Parameters**

| field | type |
|---|---|
| `inner_radius` | `Scalar` |
| `outer_radius` | `Scalar` |
| `flange_radius` | `Scalar` |
| `flange_thickness` | `Scalar` |
| `body_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "body_length": 6.0,
  "flange_radius": 4.0,
  "flange_thickness": 0.7,
  "id": "bushing",
  "inner_radius": 2.0,
  "kind": "Bushing",
  "outer_radius": 3.0,
  "segments": 16
}
```

### `Cam`

Cam: a circular disk with an offset hole — when rotated about the hole, the outer profile traces an eccentric path. `radius` is the disk outer radius, `hole_radius` the central hole, and `eccentricity` the offset of the hole from the disk's geometric center.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `hole_radius` | `Scalar` |
| `eccentricity` | `Scalar` |
| `thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "eccentricity": 1.0,
  "hole_radius": 1.5,
  "id": "cam",
  "kind": "Cam",
  "radius": 5.0,
  "segments": 16,
  "thickness": 1.0
}
```

### `CamLobe`

CamLobe: an elliptical cam disk — like Cam but with two different radii (semi-major and semi-minor), so the cam profile is an ellipse. The hole is at the geometric center (ellipse origin).

**Parameters**

| field | type |
|---|---|
| `rx` | `Scalar` |
| `ry` | `Scalar` |
| `hole_radius` | `Scalar` |
| `thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "hole_radius": 1.0,
  "id": "cam_lobe",
  "kind": "CamLobe",
  "rx": 5.0,
  "ry": 3.0,
  "segments": 16,
  "thickness": 2.0
}
```

### `CorrugatedPanel`

CorrugatedPanel: a rectangular sheet with a sequence of triangular ridges along +x. `n_ridges` ridges of `ridge_height` total ridge height (peak above valley).

**Parameters**

| field | type |
|---|---|
| `length` | `Scalar` |
| `width` | `Scalar` |
| `n_ridges` | `usize` |
| `ridge_height` | `Scalar` |
| `sheet_thickness` | `Scalar` |

**Example**

```json
{
  "id": "corrugated_panel",
  "kind": "CorrugatedPanel",
  "length": 10.0,
  "n_ridges": 4,
  "ridge_height": 0.5,
  "sheet_thickness": 1.0,
  "width": 10.0
}
```

### `Crank`

Crank: a horizontal arm with two cylinders at each end (the pivot and the wrist pin). `arm_length` is between the two pin centers; `arm_thickness` and `arm_width` define the connecting arm cross-section.

**Parameters**

| field | type |
|---|---|
| `pivot_radius` | `Scalar` |
| `wrist_radius` | `Scalar` |
| `arm_length` | `Scalar` |
| `arm_width` | `Scalar` |
| `arm_thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "arm_length": 8.0,
  "arm_thickness": 1.0,
  "arm_width": 4.0,
  "id": "crank",
  "kind": "Crank",
  "pivot_radius": 1.5,
  "segments": 16,
  "wrist_radius": 1.0
}
```

### `Hammer`

Hammer: a rectangular hammer head with a cylindrical handle below it. Head is `head_length` x `head_width` x `head_height`, centered on the xy origin, sitting at z ∈ [handle_length, handle_length + head_height]. Handle is a cylinder of `handle_radius` running from z=0 to z=handle_length + 1e-3 (pokes 1e-3 into head).

**Parameters**

| field | type |
|---|---|
| `head_length` | `Scalar` |
| `head_width` | `Scalar` |
| `head_height` | `Scalar` |
| `handle_radius` | `Scalar` |
| `handle_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "handle_length": 20.0,
  "handle_radius": 0.6,
  "head_height": 3.0,
  "head_length": 8.0,
  "head_width": 3.0,
  "id": "hammer",
  "kind": "Hammer",
  "segments": 16
}
```

### `Handle`

Handle: rectangular grip — a horizontal bar with rounded ends (cylinder caps). `bar_length` is the straight portion; total length = bar_length + 2 * cap_radius.

**Parameters**

| field | type |
|---|---|
| `bar_length` | `Scalar` |
| `bar_radius` | `Scalar` |
| `cap_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "bar_length": 10.0,
  "bar_radius": 1.0,
  "cap_radius": 2.0,
  "id": "handle",
  "kind": "Handle",
  "segments": 16
}
```

### `HookHandle`

HookHandle: a hook — straight shaft + 180° curved tip. `shaft_length` is the straight portion along +z; `bend_radius` is the radius of the U-curve at the top.

**Parameters**

| field | type |
|---|---|
| `shaft_length` | `Scalar` |
| `shaft_radius` | `Scalar` |
| `bend_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "bend_radius": 6.0,
  "id": "hook_handle",
  "kind": "HookHandle",
  "segments": 16,
  "shaft_length": 10.0,
  "shaft_radius": 2.0
}
```

### `KneadHandle`

KneadHandle: a simple grip handle — flat rectangular plate with a thicker raised pad in the center. Useful as an ergonomic grip.

**Parameters**

| field | type |
|---|---|
| `length` | `Scalar` |
| `width` | `Scalar` |
| `plate_thickness` | `Scalar` |
| `pad_radius` | `Scalar` |
| `pad_height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "knead_handle",
  "kind": "KneadHandle",
  "length": 10.0,
  "pad_height": 1.0,
  "pad_radius": 1.0,
  "plate_thickness": 1.0,
  "segments": 16,
  "width": 10.0
}
```

### `Knob`

Knob: a cylinder with axial slots ("flutes") around its perimeter — like a knurling but with deeper grooves.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `height` | `Scalar` |
| `flute_count` | `usize` |
| `flute_depth` | `Scalar` |

**Example**

```json
{
  "flute_count": 4,
  "flute_depth": 1.5,
  "height": 10.0,
  "id": "knob",
  "kind": "Knob",
  "radius": 5.0
}
```

### `KnurledGrip`

KnurledGrip: a cylinder with ridges around its circumference. The ridges are an n-sided RegularPrism slightly larger than the bare cylinder, giving a knurled / gripped look.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `ridge_height` | `Scalar` |
| `height` | `Scalar` |
| `ridge_count` | `usize` |

**Example**

```json
{
  "height": 10.0,
  "id": "knurled_grip",
  "kind": "KnurledGrip",
  "radius": 5.0,
  "ridge_count": 12,
  "ridge_height": 0.5
}
```

### `Plug`

Plug: a tapered cylinder — same as Frustum but expressed in "plug" terms (top/bottom radius, length). Useful for sealing.

**Parameters**

| field | type |
|---|---|
| `top_radius` | `Scalar` |
| `bottom_radius` | `Scalar` |
| `length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "bottom_radius": 5.0,
  "id": "plug",
  "kind": "Plug",
  "length": 10.0,
  "segments": 16,
  "top_radius": 3.0
}
```

### `Pulley`

Pulley: a cylinder with a V-groove around its outer circumference. `outer_radius` is the rim, `groove_depth` how deep the V cuts inward, `groove_width` the V opening at the rim, `width` the pulley thickness along +z.

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `groove_depth` | `Scalar` |
| `groove_width` | `Scalar` |
| `width` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "groove_depth": 1.5,
  "groove_width": 2.0,
  "id": "pulley",
  "kind": "Pulley",
  "outer_radius": 8.0,
  "segments": 16,
  "width": 10.0
}
```

### `ScrewDriver`

ScrewDriver: a fat cylindrical handle, a thinner cylindrical shaft, and a flat-blade tip. Handle of `handle_radius` and `handle_length` at z ∈ [0, handle_length]. Shaft of `shaft_radius` (< handle_radius) at z ∈ [handle_length - 1e-3, handle_length + shaft_length]. Tip is a thin flat box of `tip_width` x `tip_thickness` x `tip_length` at the shaft's end.

**Parameters**

| field | type |
|---|---|
| `handle_radius` | `Scalar` |
| `handle_length` | `Scalar` |
| `shaft_radius` | `Scalar` |
| `shaft_length` | `Scalar` |
| `tip_width` | `Scalar` |
| `tip_thickness` | `Scalar` |
| `tip_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "handle_length": 8.0,
  "handle_radius": 2.5,
  "id": "screw_driver",
  "kind": "ScrewDriver",
  "segments": 16,
  "shaft_length": 15.0,
  "shaft_radius": 0.5,
  "tip_length": 1.0,
  "tip_thickness": 0.3,
  "tip_width": 1.5
}
```

### `SerratedDisk`

SerratedDisk: a thin disk with very fine teeth — like a crown / star washer. `tooth_count` should be high (24+).

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `root_radius` | `Scalar` |
| `tooth_count` | `usize` |
| `thickness` | `Scalar` |

**Example**

```json
{
  "id": "serrated_disk",
  "kind": "SerratedDisk",
  "outer_radius": 8.0,
  "root_radius": 7.0,
  "thickness": 2.0,
  "tooth_count": 12
}
```

### `Spike`

Spike: a cone (faceted) — apex at +z, base at z=0. Same as Cone but expressed as Spike for catalogue clarity.

**Parameters**

| field | type |
|---|---|
| `base_radius` | `Scalar` |
| `height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "base_radius": 8.0,
  "height": 10.0,
  "id": "spike",
  "kind": "Spike",
  "segments": 16
}
```

### `Sprocket`

Sprocket: a cylinder with `tooth_count` triangular teeth around its perimeter. Same parameter shape as GearBlank but with a triangular (instead of rectangular) tooth profile.

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `root_radius` | `Scalar` |
| `tooth_count` | `usize` |
| `thickness` | `Scalar` |

**Example**

```json
{
  "id": "sprocket",
  "kind": "Sprocket",
  "outer_radius": 8.0,
  "root_radius": 7.0,
  "thickness": 2.0,
  "tooth_count": 12
}
```

### `Tetrahedron`

Tetrahedron: regular 4-faced pyramid with equilateral triangle base. `edge_length` is the side length of every edge.

**Parameters**

| field | type |
|---|---|
| `edge_length` | `Scalar` |

**Example**

```json
{
  "edge_length": 8.0,
  "id": "tetrahedron",
  "kind": "Tetrahedron"
}
```

### `ToroidalKnob`

ToroidalKnob: a knob with a toroidal grip ridge — cylinder body with a faceted-torus rib around its top.

**Parameters**

| field | type |
|---|---|
| `body_radius` | `Scalar` |
| `body_height` | `Scalar` |
| `torus_major_radius` | `Scalar` |
| `torus_minor_radius` | `Scalar` |
| `body_segs` | `usize` |
| `torus_segs` | `usize` |

**Example**

```json
{
  "body_height": 10.0,
  "body_radius": 5.0,
  "body_segs": 16,
  "id": "toroidal_knob",
  "kind": "ToroidalKnob",
  "torus_major_radius": 5.0,
  "torus_minor_radius": 1.0,
  "torus_segs": 16
}
```

### `Wrench`

Wrench: a rectangular head with a centered jaw cutout, joined to a tapered handle. The head is `head_width` x `head_length` x `thickness`, sitting at one end. The handle is a tapered prism (frustum-as-extrude) running from `head_length` along +y for `handle_length`, narrowing from `handle_root_width` to `handle_tip_width`. (No actual jaw cutout in this MVP — implemented as a solid rectangular head; jaw cutout is future work.)

**Parameters**

| field | type |
|---|---|
| `head_width` | `Scalar` |
| `head_length` | `Scalar` |
| `thickness` | `Scalar` |
| `handle_root_width` | `Scalar` |
| `handle_tip_width` | `Scalar` |
| `handle_length` | `Scalar` |

**Example**

```json
{
  "handle_length": 1.0,
  "handle_root_width": 1.0,
  "handle_tip_width": 1.0,
  "head_length": 1.0,
  "head_width": 1.0,
  "id": "wrench",
  "kind": "Wrench",
  "thickness": 1.0
}
```

## Reference geometry & markers

### `AnchorPoint`

AnchorPoint: a clip-style anchor — a flat plate with a projecting tab and a hole through the tab.

**Parameters**

| field | type |
|---|---|
| `plate_width` | `Scalar` |
| `plate_height` | `Scalar` |
| `plate_thickness` | `Scalar` |
| `tab_height` | `Scalar` |
| `hole_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "hole_radius": 1.5,
  "id": "anchor_point",
  "kind": "AnchorPoint",
  "plate_height": 10.0,
  "plate_thickness": 1.0,
  "plate_width": 10.0,
  "segments": 16,
  "tab_height": 10.0
}
```

### `AngleArc`

Reference: angular arc — a thin curved rod sweeping through a given angle in the xy plane. Used for visualising angle relationships. Center at (cx, cy, z), radius `r`, from angle `start_deg` to `start_deg + sweep_deg`.

**Parameters**

| field | type |
|---|---|
| `center` | `[Scalar; 3]` |
| `radius` | `Scalar` |
| `start_deg` | `Scalar` |
| `sweep_deg` | `Scalar` |
| `rod_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "center": [
    0.0,
    0.0,
    0.0
  ],
  "id": "angle_arc",
  "kind": "AngleArc",
  "radius": 5.0,
  "rod_radius": 2.0,
  "segments": 16,
  "start_deg": 0.0,
  "sweep_deg": 90.0
}
```

### `Arrow`

Arrow: a cone-shaped tip on top of a cylindrical shaft. Useful for axis indicators. Total length = `shaft_length + tip_length`. The tip cone tapers from `shaft_radius` at z = shaft_length down to a point at z = shaft_length + tip_length. (Same radius at the junction avoids the multi-cylinder coplanar-flare issue that would otherwise trip the boolean engine.)

**Parameters**

| field | type |
|---|---|
| `shaft_radius` | `Scalar` |
| `shaft_length` | `Scalar` |
| `tip_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "arrow",
  "kind": "Arrow",
  "segments": 16,
  "shaft_length": 10.0,
  "shaft_radius": 2.0,
  "tip_length": 3.0
}
```

### `BoundingBoxRef`

Reference: bounding-box-of-a-feature marker. Computes axis-aligned bounding box of the input feature's solid; renders as a thin wire box. Useful for visualisation and downstream constraint references.

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `wire_thickness` | `Scalar` |

**Example**

```json
{
  "id": "bounding_box_ref",
  "input": "body",
  "kind": "BoundingBoxRef",
  "wire_thickness": 0.3
}
```

### `CentroidPoint`

Reference: centroid of a feature, rendered as a small octahedron at the centroid position. Computed as the volume-weighted centroid of the input solid's tessellated triangles.

**Parameters**

| field | type |
|---|---|
| `input` | `String` |
| `marker_size` | `Scalar` |

**Example**

```json
{
  "id": "centroid_point",
  "input": "body",
  "kind": "CentroidPoint",
  "marker_size": 0.5
}
```

### `DistanceRod`

Reference: a thin rod between two world-space points, drawn as a small-radius cylinder. Useful for visualising distances and constraint relationships in the rendered model.

**Parameters**

| field | type |
|---|---|
| `from` | `[Scalar; 3]` |
| `to` | `[Scalar; 3]` |
| `radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "from": [
    0.0,
    0.0,
    0.0
  ],
  "id": "distance_rod",
  "kind": "DistanceRod",
  "radius": 5.0,
  "segments": 16,
  "to": [
    10.0,
    0.0,
    0.0
  ]
}
```

### `Marker3D`

Marker3D: a 6-axis cross marker — three perpendicular thin bars joined at the origin. Useful as a reference indicator at any point in space.

**Parameters**

| field | type |
|---|---|
| `center` | `[Scalar; 3]` |
| `axis_length` | `Scalar` |
| `bar_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "axis_length": 1.0,
  "bar_radius": 1.0,
  "center": [
    0.0,
    0.0,
    0.0
  ],
  "id": "marker3_d",
  "kind": "Marker3D",
  "segments": 16
}
```

### `RefAxis`

Reference axis marker: a thin faceted cylinder running along `axis` ("x" | "y" | "z") for `length`, centered at `position`. Visual aid for marking axes (rotation axes, alignment lines).

**Parameters**

| field | type |
|---|---|
| `position` | `[Scalar; 3]` |
| `axis` | `String` |
| `length` | `Scalar` |
| `marker_radius` | `Scalar` |

**Example**

```json
{
  "axis": "z",
  "id": "ref_axis",
  "kind": "RefAxis",
  "length": 10.0,
  "marker_radius": 0.3,
  "position": [
    0.0,
    0.0,
    0.0
  ]
}
```

### `RefPlane`

Reference plane marker: a thin faceted box marking a planar region. `position` is the center; `axis` is the normal direction ("x" | "y" | "z"); `extents` is the (width, height) of the rectangular marker (in the perpendicular axes); `marker_thickness` is the thickness along the normal.

**Parameters**

| field | type |
|---|---|
| `position` | `[Scalar; 3]` |
| `axis` | `String` |
| `extents` | `[Scalar; 2]` |
| `marker_thickness` | `Scalar` |

**Example**

```json
{
  "axis": "z",
  "extents": [
    10.0,
    10.0
  ],
  "id": "ref_plane",
  "kind": "RefPlane",
  "marker_thickness": 1.0,
  "position": [
    0.0,
    0.0,
    0.0
  ]
}
```

### `RefPoint`

Reference point marker: a tiny faceted sphere at `position` of `marker_radius`. Visual aid for marking locations in a model (mounting holes, datum points, etc.). Does not bind to constraints.

**Parameters**

| field | type |
|---|---|
| `position` | `[Scalar; 3]` |
| `marker_radius` | `Scalar` |

**Example**

```json
{
  "id": "ref_point",
  "kind": "RefPoint",
  "marker_radius": 0.3,
  "position": [
    0.0,
    0.0,
    0.0
  ]
}
```

### `VectorArrow`

VectorArrow: a 3D vector visualization — a thin cylinder shaft from `from` to (very near) `to`, capped by a small cone tip pointing at `to`.

**Parameters**

| field | type |
|---|---|
| `from` | `[Scalar; 3]` |
| `to` | `[Scalar; 3]` |
| `shaft_radius` | `Scalar` |
| `head_radius` | `Scalar` |
| `head_length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "from": [
    0.0,
    0.0,
    0.0
  ],
  "head_length": 1.0,
  "head_radius": 4.0,
  "id": "vector_arrow",
  "kind": "VectorArrow",
  "segments": 16,
  "shaft_radius": 2.0,
  "to": [
    10.0,
    0.0,
    0.0
  ]
}
```

## Decorative & miscellaneous shapes

### `AcornShape`

AcornShape: a stylised acorn — sphere body topped by a small cylindrical stem. Two pieces unioned.

**Parameters**

| field | type |
|---|---|
| `body_radius` | `Scalar` |
| `stem_radius` | `Scalar` |
| `stem_length` | `Scalar` |
| `stacks` | `usize` |
| `slices` | `usize` |

**Example**

```json
{
  "body_radius": 5.0,
  "id": "acorn_shape",
  "kind": "AcornShape",
  "slices": 16,
  "stacks": 16,
  "stem_length": 1.0,
  "stem_radius": 1.0
}
```

### `ArcSegment`

ArcSegment: a partial torus arc — like a Donut sliced by an angular range. Approximated using AngleArc (chained cylinders).

**Parameters**

| field | type |
|---|---|
| `major_radius` | `Scalar` |
| `minor_radius` | `Scalar` |
| `start_deg` | `Scalar` |
| `sweep_deg` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "arc_segment",
  "kind": "ArcSegment",
  "major_radius": 5.0,
  "minor_radius": 1.0,
  "segments": 16,
  "start_deg": 0.0,
  "sweep_deg": 90.0
}
```

### `Bishop`

Bishop: chess bishop — cylinder body + sphere head + thin slot across the head (the bishop's traditional "mitre slot").

**Parameters**

| field | type |
|---|---|
| `base_radius` | `Scalar` |
| `base_height` | `Scalar` |
| `body_radius` | `Scalar` |
| `body_height` | `Scalar` |
| `head_radius` | `Scalar` |
| `slot_width` | `Scalar` |
| `segments` | `usize` |
| `stacks` | `usize` |

**Example**

```json
{
  "base_height": 2.0,
  "base_radius": 6.0,
  "body_height": 10.0,
  "body_radius": 3.0,
  "head_radius": 3.5,
  "id": "bishop",
  "kind": "Bishop",
  "segments": 16,
  "slot_width": 1.0,
  "stacks": 4
}
```

### `BoneShape`

BoneShape: an elongated capsule with bulbous ends — like a dumbbell. Two SphereFaceted ends + cylindrical shaft.

**Parameters**

| field | type |
|---|---|
| `end_radius` | `Scalar` |
| `shaft_radius` | `Scalar` |
| `shaft_length` | `Scalar` |
| `stacks` | `usize` |
| `slices` | `usize` |

**Example**

```json
{
  "end_radius": 3.0,
  "id": "bone_shape",
  "kind": "BoneShape",
  "shaft_length": 10.0,
  "shaft_radius": 1.5,
  "slices": 8,
  "stacks": 4
}
```

### `Bowtie`

Bowtie: two triangles joined at a central vertex, lying in xy plane and extruded along +z. `width` is the total span along +x, `height` the span along +y, `pinch` the gap at center.

**Parameters**

| field | type |
|---|---|
| `width` | `Scalar` |
| `height` | `Scalar` |
| `pinch` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "depth": 10.0,
  "height": 10.0,
  "id": "bowtie",
  "kind": "Bowtie",
  "pinch": 1.0,
  "width": 10.0
}
```

### `BulletShape`

BulletShape: a cylinder with a hemispherical cap on top — the classic projectile profile.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `body_length` | `Scalar` |
| `stacks` | `usize` |
| `slices` | `usize` |

**Example**

```json
{
  "body_length": 10.0,
  "id": "bullet_shape",
  "kind": "BulletShape",
  "radius": 5.0,
  "slices": 16,
  "stacks": 16
}
```

### `ButtonShape`

ButtonShape: a flat disk with rounded outer edges — like a real button. Implemented as a thin cylinder. Visualised as a flat disk rather than truly rounded since real edge rounds need Shell-class kernel work.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "button_shape",
  "kind": "ButtonShape",
  "radius": 5.0,
  "segments": 16,
  "thickness": 1.0
}
```

### `CircularRing`

CircularRing: a thin flat ring — annulus extruded along +z. Like Tube but typically very short (a thin "washer-shaped" ring).

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `inner_radius` | `Scalar` |
| `thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "circular_ring",
  "inner_radius": 4.0,
  "kind": "CircularRing",
  "outer_radius": 8.0,
  "segments": 16,
  "thickness": 1.0
}
```

### `CoinShape`

CoinShape: a flat disk with a raised rim around its edge. `outer_radius` is the disk radius, `face_thickness` the flat portion's thickness, `rim_height` how much the rim projects above the face, `rim_width` the rim's radial width.

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `face_thickness` | `Scalar` |
| `rim_height` | `Scalar` |
| `rim_width` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "face_thickness": 1.0,
  "id": "coin_shape",
  "kind": "CoinShape",
  "outer_radius": 8.0,
  "rim_height": 1.0,
  "rim_width": 1.0,
  "segments": 16
}
```

### `Crescent`

Crescent: two overlapping disks where the smaller disk is subtracted from the larger to form a crescent moon shape. Both disks lie in xy plane, extruded along +z. The small disk is offset by `offset` from the large disk's center.

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `inner_radius` | `Scalar` |
| `offset` | `Scalar` |
| `thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "crescent",
  "inner_radius": 6.0,
  "kind": "Crescent",
  "offset": 3.0,
  "outer_radius": 8.0,
  "segments": 16,
  "thickness": 1.0
}
```

### `Cross3D`

Cross3D: a 3D Christian cross — a vertical box of (width x depth x height) and a horizontal crossbar of (arm_span x depth x arm_height) at z = arm_z. The crossbar is centered on x. The vertical post sits at x ∈ [0, width] (with arm_span > width, the crossbar overhangs symmetrically).

**Parameters**

| field | type |
|---|---|
| `width` | `Scalar` |
| `depth` | `Scalar` |
| `height` | `Scalar` |
| `arm_span` | `Scalar` |
| `arm_height` | `Scalar` |
| `arm_z` | `Scalar` |

**Example**

```json
{
  "arm_height": 2.0,
  "arm_span": 8.0,
  "arm_z": 6.0,
  "depth": 2.0,
  "height": 10.0,
  "id": "cross3_d",
  "kind": "Cross3D",
  "width": 2.0
}
```

### `CylindricalCap`

CylindricalCap: top half of a cylinder, sliced by a vertical plane through the axis. Useful for half-pipe shapes.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `length` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "cylindrical_cap",
  "kind": "CylindricalCap",
  "length": 10.0,
  "radius": 5.0,
  "segments": 16
}
```

### `Diabolo`

Diabolo: an inverted hourglass — wide in the middle, narrow at the ends. Like Hourglass but with the waist and end radii swapped.

**Parameters**

| field | type |
|---|---|
| `end_radius` | `Scalar` |
| `waist_radius` | `Scalar` |
| `half_height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "end_radius": 2.0,
  "half_height": 5.0,
  "id": "diabolo",
  "kind": "Diabolo",
  "segments": 16,
  "waist_radius": 5.0
}
```

### `DiskWithSlots`

DiskWithSlots: a disk with N evenly-spaced radial rectangular slots cut from the rim. Used for slotted couplings, wheels.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `slot_count` | `usize` |
| `slot_width` | `Scalar` |
| `slot_depth` | `Scalar` |
| `thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "disk_with_slots",
  "kind": "DiskWithSlots",
  "radius": 5.0,
  "segments": 16,
  "slot_count": 4,
  "slot_depth": 1.0,
  "slot_width": 10.0,
  "thickness": 1.0
}
```

### `DonutSlice`

DonutSlice: a wedge of a faceted donut — like an orange slice of a torus. Defined by sweep angle (0 to 360 degrees).

**Parameters**

| field | type |
|---|---|
| `major_radius` | `Scalar` |
| `minor_radius` | `Scalar` |
| `sweep_deg` | `Scalar` |
| `major_segs` | `usize` |
| `minor_segs` | `usize` |

**Example**

```json
{
  "id": "donut_slice",
  "kind": "DonutSlice",
  "major_radius": 5.0,
  "major_segs": 16,
  "minor_radius": 1.0,
  "minor_segs": 16,
  "sweep_deg": 90.0
}
```

### `EggShape`

EggShape: a stretched faceted sphere — `radius` is the equatorial radius, `aspect_z` scales along z (>1 prolate, <1 oblate).

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `aspect_z` | `Scalar` |
| `stacks` | `usize` |
| `slices` | `usize` |

**Example**

```json
{
  "aspect_z": 1.5,
  "id": "egg_shape",
  "kind": "EggShape",
  "radius": 5.0,
  "slices": 16,
  "stacks": 16
}
```

### `Ellipsoid3D`

Ellipsoid3D: a general triaxial ellipsoid. Built as a faceted sphere of `radius=1` then scaled by `(rx, ry, rz)`.

**Parameters**

| field | type |
|---|---|
| `rx` | `Scalar` |
| `ry` | `Scalar` |
| `rz` | `Scalar` |
| `stacks` | `usize` |
| `slices` | `usize` |

**Example**

```json
{
  "id": "ellipsoid3_d",
  "kind": "Ellipsoid3D",
  "rx": 1.0,
  "ry": 1.0,
  "rz": 1.0,
  "slices": 16,
  "stacks": 16
}
```

### `FivePointedBadge`

FivePointedBadge: a 5-pointed star plate (alias of Star with fixed 5 points and explicit "badge" intent).

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `inner_radius` | `Scalar` |
| `thickness` | `Scalar` |

**Example**

```json
{
  "id": "five_pointed_badge",
  "inner_radius": 4.0,
  "kind": "FivePointedBadge",
  "outer_radius": 8.0,
  "thickness": 1.0
}
```

### `HalfTorus`

HalfTorus: a 180° torus arc.

**Parameters**

| field | type |
|---|---|
| `major_radius` | `Scalar` |
| `minor_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "half_torus",
  "kind": "HalfTorus",
  "major_radius": 5.0,
  "minor_radius": 1.0,
  "segments": 16
}
```

### `Heart`

Heart: a heart-shaped extruded plate. `size` is the bounding box edge length, `thickness` the extrusion depth.

**Parameters**

| field | type |
|---|---|
| `size` | `Scalar` |
| `thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "heart",
  "kind": "Heart",
  "segments": 16,
  "size": 1.0,
  "thickness": 1.0
}
```

### `Heart3D`

Heart3D: a 3D heart shape — two faceted spheres for the lobes joined to a downward-pointing cone for the bottom point. Lobe spheres of `lobe_radius` are placed at (±lobe_offset, 0, lobe_z). The cone has its apex at (0, 0, 0) and its base of `lobe_radius` at z=lobe_z. Uses the AcornShape pole-overlap pattern for sphere unions.

**Parameters**

| field | type |
|---|---|
| `lobe_radius` | `Scalar` |
| `lobe_offset` | `Scalar` |
| `lobe_z` | `Scalar` |
| `stacks` | `usize` |
| `slices` | `usize` |

**Example**

```json
{
  "id": "heart3_d",
  "kind": "Heart3D",
  "lobe_offset": 1.0,
  "lobe_radius": 1.0,
  "lobe_z": 1.0,
  "slices": 16,
  "stacks": 16
}
```

### `Hourglass`

Hourglass: two frustums meeting waist-to-waist — wide at top and bottom, narrow in the middle. Symmetric: same radii at top and bottom, single `waist_radius` at center.

**Parameters**

| field | type |
|---|---|
| `end_radius` | `Scalar` |
| `waist_radius` | `Scalar` |
| `half_height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "end_radius": 5.0,
  "half_height": 5.0,
  "id": "hourglass",
  "kind": "Hourglass",
  "segments": 16,
  "waist_radius": 2.0
}
```

### `KeyholeShape`

KeyholeShape: a keyhole-shaped extruded plate — circular hole at the top with a rectangular slot beneath it. The classic keyhole silhouette.

**Parameters**

| field | type |
|---|---|
| `circle_radius` | `Scalar` |
| `slot_width` | `Scalar` |
| `slot_height` | `Scalar` |
| `plate_width` | `Scalar` |
| `plate_height` | `Scalar` |
| `plate_thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "circle_radius": 2.0,
  "id": "keyhole_shape",
  "kind": "KeyholeShape",
  "plate_height": 12.0,
  "plate_thickness": 1.0,
  "plate_width": 8.0,
  "segments": 16,
  "slot_height": 4.0,
  "slot_width": 1.5
}
```

### `Lens`

Lens: a biconvex lens shape — two spherical caps joined back to back at z=0. `radius` is the sphere's radius, `cap_height` is each cap's height, so total lens thickness = 2 * cap_height.

**Parameters**

| field | type |
|---|---|
| `radius` | `Scalar` |
| `cap_height` | `Scalar` |
| `stacks` | `usize` |
| `slices` | `usize` |

**Example**

```json
{
  "cap_height": 2.0,
  "id": "lens",
  "kind": "Lens",
  "radius": 5.0,
  "slices": 16,
  "stacks": 16
}
```

### `NameTag`

NameTag: a rounded-corner rectangular plate with a small circular hanging hole near the top edge.

**Parameters**

| field | type |
|---|---|
| `width` | `Scalar` |
| `height` | `Scalar` |
| `thickness` | `Scalar` |
| `corner_radius` | `Scalar` |
| `hole_radius` | `Scalar` |
| `hole_offset_from_top` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "corner_radius": 1.5,
  "height": 10.0,
  "hole_offset_from_top": 2.0,
  "hole_radius": 0.8,
  "id": "name_tag",
  "kind": "NameTag",
  "segments": 12,
  "thickness": 1.0,
  "width": 20.0
}
```

### `Pawn`

Pawn: chess pawn — sphere on a tapered cylindrical body atop a disk base. Stylised, not anatomically correct.

**Parameters**

| field | type |
|---|---|
| `base_radius` | `Scalar` |
| `base_height` | `Scalar` |
| `body_top_radius` | `Scalar` |
| `body_height` | `Scalar` |
| `head_radius` | `Scalar` |
| `segments` | `usize` |
| `stacks` | `usize` |

**Example**

```json
{
  "base_height": 10.0,
  "base_radius": 8.0,
  "body_height": 10.0,
  "body_top_radius": 1.0,
  "head_radius": 4.0,
  "id": "pawn",
  "kind": "Pawn",
  "segments": 16,
  "stacks": 16
}
```

### `PolygonRing`

PolygonRing: a regular n-sided ring — outer regular polygon minus inner regular polygon, extruded along +z.

**Parameters**

| field | type |
|---|---|
| `outer_radius` | `Scalar` |
| `inner_radius` | `Scalar` |
| `sides` | `usize` |
| `thickness` | `Scalar` |

**Example**

```json
{
  "id": "polygon_ring",
  "inner_radius": 4.0,
  "kind": "PolygonRing",
  "outer_radius": 8.0,
  "sides": 4,
  "thickness": 1.0
}
```

### `QuarterTorus`

QuarterTorus: a 90° torus arc. Same as ArcSegment but with fixed sweep_deg = 90.

**Parameters**

| field | type |
|---|---|
| `major_radius` | `Scalar` |
| `minor_radius` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "quarter_torus",
  "kind": "QuarterTorus",
  "major_radius": 5.0,
  "minor_radius": 1.0,
  "segments": 16
}
```

### `Rook`

Rook: chess rook — cylinder body on disk base, with crenellated top (similar to ParapetWall but radial).

**Parameters**

| field | type |
|---|---|
| `base_radius` | `Scalar` |
| `base_height` | `Scalar` |
| `body_radius` | `Scalar` |
| `body_height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "base_height": 10.0,
  "base_radius": 8.0,
  "body_height": 10.0,
  "body_radius": 5.0,
  "id": "rook",
  "kind": "Rook",
  "segments": 16
}
```

### `SquaredRing`

SquaredRing: rectangular outer outline minus rectangular inner outline (both axis-aligned), extruded along +z.

**Parameters**

| field | type |
|---|---|
| `outer_width` | `Scalar` |
| `outer_height` | `Scalar` |
| `wall_thickness` | `Scalar` |
| `depth` | `Scalar` |

**Example**

```json
{
  "depth": 10.0,
  "id": "squared_ring",
  "kind": "SquaredRing",
  "outer_height": 10.0,
  "outer_width": 10.0,
  "wall_thickness": 1.0
}
```

### `StadiumPlate`

StadiumPlate: flat stadium-shaped plate — same outline as Slot3D but framed as a "plate" (very thin) instead of a "slot".

**Parameters**

| field | type |
|---|---|
| `length` | `Scalar` |
| `width` | `Scalar` |
| `thickness` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "id": "stadium_plate",
  "kind": "StadiumPlate",
  "length": 10.0,
  "segments": 16,
  "thickness": 1.0,
  "width": 10.0
}
```

### `Star`

Regular n-pointed star prism: profile is a star polygon with `points` outer tips at `outer_radius` and `points` inner valleys at `inner_radius`, alternating. Extruded along +z by `height`. `inner_radius` must be < `outer_radius`.

**Parameters**

| field | type |
|---|---|
| `points` | `usize` |
| `outer_radius` | `Scalar` |
| `inner_radius` | `Scalar` |
| `height` | `Scalar` |

**Example**

```json
{
  "height": 10.0,
  "id": "star",
  "inner_radius": 4.0,
  "kind": "Star",
  "outer_radius": 8.0,
  "points": 6
}
```

### `Star3D`

Star3D: a 3D 6-pointed star shape — three orthogonal elongated boxes crossing at the origin, forming a 3D plus / asterisk. Each arm is a box of `arm_length` x `arm_thickness` x `arm_thickness`, centered on origin, oriented along x, y, and z respectively.

**Parameters**

| field | type |
|---|---|
| `arm_length` | `Scalar` |
| `arm_thickness` | `Scalar` |

**Example**

```json
{
  "arm_length": 10.0,
  "arm_thickness": 1.0,
  "id": "star3_d",
  "kind": "Star3D"
}
```

### `TripleStep`

TripleStep: three stacked cylindrical tiers of decreasing radius (a wedding-cake or stepped pyramid silhouette).

**Parameters**

| field | type |
|---|---|
| `bottom_radius` | `Scalar` |
| `bottom_height` | `Scalar` |
| `middle_radius` | `Scalar` |
| `middle_height` | `Scalar` |
| `top_radius` | `Scalar` |
| `top_height` | `Scalar` |
| `segments` | `usize` |

**Example**

```json
{
  "bottom_height": 2.0,
  "bottom_radius": 8.0,
  "id": "triple_step",
  "kind": "TripleStep",
  "middle_height": 2.0,
  "middle_radius": 5.0,
  "segments": 16,
  "top_height": 2.0,
  "top_radius": 3.0
}
```

### `Volute`

Volute: a decorative scroll/spiral disk — Archimedean spiral rod plus a central disk. Used as classical column-capital ornament.

**Parameters**

| field | type |
|---|---|
| `max_radius` | `Scalar` |
| `revolutions` | `Scalar` |
| `rod_radius` | `Scalar` |
| `center_disk_radius` | `Scalar` |
| `thickness` | `Scalar` |
| `segments_per_revolution` | `usize` |
| `center_segments` | `usize` |

**Example**

```json
{
  "center_disk_radius": 1.5,
  "center_segments": 12,
  "id": "volute",
  "kind": "Volute",
  "max_radius": 8.0,
  "revolutions": 2.0,
  "rod_radius": 0.4,
  "segments_per_revolution": 12,
  "thickness": 1.0
}
```

### `WaveProfile`

WaveProfile: a sinusoidal-approximated wave plate — N peaks approximated as triangle ridges (like CorrugatedPanel) but with the profile in the xy plane and extruded along +z. Different orientation than CorrugatedPanel.

**Parameters**

| field | type |
|---|---|
| `wavelength` | `Scalar` |
| `amplitude` | `Scalar` |
| `n_waves` | `usize` |
| `depth` | `Scalar` |
| `height` | `Scalar` |

**Example**

```json
{
  "amplitude": 1.0,
  "depth": 10.0,
  "height": 10.0,
  "id": "wave_profile",
  "kind": "WaveProfile",
  "n_waves": 4,
  "wavelength": 1.0
}
```

### `ZigzagBar`

ZigzagBar: a horizontal bar with a triangular zigzag profile along its top — like a saw blade silhouette extruded.

**Parameters**

| field | type |
|---|---|
| `length` | `Scalar` |
| `depth` | `Scalar` |
| `base_height` | `Scalar` |
| `zigzag_height` | `Scalar` |
| `n_zigs` | `usize` |

**Example**

```json
{
  "base_height": 10.0,
  "depth": 10.0,
  "id": "zigzag_bar",
  "kind": "ZigzagBar",
  "length": 10.0,
  "n_zigs": 4,
  "zigzag_height": 1.0
}
```

